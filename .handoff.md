# Session Handoff

> ⚠️ **STANDING RULE — UPDATE THE HANDOFF EVERY SESSION.** Before you end any
> session, update BOTH this file (`.handoff.md`, the short per-session baton) and
> `HANDOFF.md` (the persistent guide — its top status block + the "Prioritized
> non-asset backlog"): record what shipped, the gate results (tests/tsc/bench/
> determinism), any intentional re-baseline, gotchas/failed paths, and the next
> step. Do this as the final commit even when the feature already merged — the
> handoff IS the deliverable for the next session. Don't skip it.

**Session date:** 2026-07-01 (session 16) · **Repo:** aslater100/Centuria · **Branch:** claude/parallel-sessions-workflow-p9eayx · **Tests:** 1393 passing (was 1389) · tsc green · default 8-seed sweep BYTE-IDENTICAL to the session-15 baseline · bench PASS (worst 6.9ms) · **THIS SESSION — THE GILDED CENTURY: full-game AAA UI overhaul + World Dynamism campaign options + new-mechanics UI (5 commits).** User: *"now work on the UI for all these new options and game mechanics. make it look great (proper window sizes… windows should grow or shrink… no wrapped sentences… Real AAA game look… bright colors, words easy to read… plus and minus buttons… slider bars… text input. any questions?"* Asked 3 scoping questions (AskUserQuestion): answers = **full-game overhaul**, **expose the sim flags as new-game options**, **Civ VI gold-and-navy direction**. **Loop I ran:** 2 parallel Explore scouts (UI framework + full panel inventory w/ feature seams) → I hand-built the design-system core (taste-setting) → **3 parallel implementation agents on disjoint file sets** (style.css family sweep / regionview.ts panels+mechanics / menus+options+sim wiring — zero conflicts, each committed only its owned files) → my integration pass: Playwright screenshot loop against the real app (scripts/shoot-ui.mjs, NEW — the visual gate) → fix round → gates → handoff. **KEY SCOUT FINDING:** the game's only token system (the `.cv-app` "4X MODERN UI THEME" block, ~550 lines with all the CSS vars) was DEAD CODE — never applied to the live DOM; the live UI was scattered light-indigo classes with fixed pixel widths + `word-break:break-word` (the literal wrapped-sentences problem). **SHIPPED:** (1) **16721cd design-system core** — live `:root` tokens ("Gilded Century": navy surfaces/gold gilding/parchment text/bright semantics/shadows/serif display type), global brass scrollbars, unified control livery (navy-plate buttons + gilded `.btn-gold` primaries with press states, inset input/select wells, brass-thumb sliders, `.stepper` +/− keys, `.panel-title` nameplates, `.meter` bars m-good/m-info/m-bad), and the window sizing model: `width:max-content` + min/max caps + styled scroll, prose capped at 56ch — no more break-word. (2) **4873c56 World Dynamism** — scenario screen section with 2 toggle switches ('Living World Market' = consumerDemand, 'A World That Fights Back' = rivalClimateResponse, default OFF), applied at all 4 RegionSim construction sites in main.ts; **the flags are now SERIALIZED campaign state** (`?? false` restore; the rival-climate-response persistence test intentionally FLIPPED + tests/world-dynamism.test.ts +4; headless env flags still override — sweeps unaffected); designscreen.ts de-inlined to classes. (3) **fae326e stylesheet sweep** — every family on tokens (event/win/ceremony/menu/pause/wiki/gameover/crisis/buildbar/bottombar/research-tt/route/central-bank/title screen…), all 8 fixed-width panels converted to grow-to-content, `break-word` eliminated, **dead .cv-app block deleted** (live .cv-* classes ported top-level; latent `--text-muted` undefined-var bug fixed), era skins enriched (per-era top-caps + nameplate tints). (4) **abb88c0 regionview adoption** — inline styles 132→15 (survivors are data: meter fills, categorical colors, DAG coords); steppers on services/militia/per-town tax (same ids, wiring untouched); ALL bars → `.meter` via `meterBar()` helper; panel-title on 9 panels; **ARSENAL readout** in war room + Economy→Supply (armamentsCapacity meter + steel/chem stocks + strain hint); **intel-gated rival arms line** (⚒ Arms base N%, only when rivalClimateResponse && intel ≥ 0.5) in rival ledger rows; **Economy 4th tab 'Wonders'**: 🚀 THE SPACE RACE group + classic wonders with owner/★prestige/unlock-year/gold UNCLAIMED badges. (5) **204d773 scenario cards + scripts/shoot-ui.mjs** — the scenario list classes had NO styles anywhere (rendered as one run-together blob — pre-existing, exposed by the restyle); now proper cards (gold-rimmed selection, era chips, skull difficulty). The harness drives title→scenarios→campaign→every panel and screenshots each state. **GATES:** tsc clean; 1393/1393 (85 files); default sweep byte-identical (serialization is additive keys only; headless overrides unchanged); bench 6.9ms; 10 Playwright screenshots verified by eye (title/scenarios/region/economy/wonders/research/settlements/routes/inspector/pause — all clean, no console errors). **GOTCHAS:** (a) regionview panel "hotkeys" E/S/R don't exist — they're REGION-panel toggle buttons (#economy-toggle/#settlements-toggle/#routenet-toggle); only T and B are real keys (main.ts). (b) `drawRivalPanel` shows in-region RegionalFactions, NOT RivalNations — rivalArmsCapacity/intelOf don't apply to that id space (the arms line lives in the rival ledger rows only; the Leg-2 id-space unification rock strikes again). (c) The left-anchored panels all stack at left:16px top:56px — screenshot harness nudges them; a future cascade/offset in WindowManager would be a nice touch. (d) ESM scripts can't run from /tmp (node_modules resolution) — scripts/shoot-ui.mjs lives in-repo. **NEXT (UI follow-ons):** panel open/close transitions + WindowManager title-bar drag handles & cascade offsets; number-input +/− steppers on recruit/deal modals; canvas-side map palette still bright/washed vs the navy chrome (a matter of taste — ask the user); era-skin deepening (backdrop tint from live metrics — the old backlog item); Save Game double-gold in pause menu (minor hierarchy nit). Sim NEXT unchanged from session 15 (arms-capacity in rivalWarPower, inc-4 outlays, default-flip re-eval).

**Session date:** 2026-07-01 (session 15) · **Repo:** aslater100/Centuria · **Branch:** claude/parallel-sessions-workflow-p9eayx · **Tests:** 1389 passing (was 1365) · tsc green · serialize-determinism BYTE-IDENTICAL · save-size green · bench PASS (worst 11.2ms solo) · **THIS SESSION — THE WORLD'S ARSENALS: rival wars become scarcity-decided, wars physically drain the arsenals, and the century's final race leaves the ground (economy-realism arc inc 3; 3 feature commits + handoff).** User: *"continue the work, run multiple parallel sessions… deeply. agentic. accurate. Compress often. Avoid token bloat. Create a loop…"* **Loop I ran (the parallel-sessions distinction):** understand-Workflow (4 parallel Explore scouts → line-anchored maps of rival-war/goods-draw/wonder/gate-harness seams) → design → PARALLEL implement — 2 interlocking region.ts features in the main context while a worktree-isolated agent shipped the pure-data space wonders (zero file overlap, clean cherry-pick) → gate → verify-Workflow (3 adversarial skeptics, **3/3 refuted=false**, two of them independently rebuilt pre/post worktrees and re-proved byte-identity empirically) → handoff. Deliberately SKIPPED (less-is-more): inc-4 govt outlays (playtest-sensitive tuning, wrong to batch with 3 features). **SHIPPED (3 commits):** (1) **3b49ca4 rival-side arms base** — `rivalArmsCapacity(r,rv)` (region.ts, beside the climate-response block) = clamp01(`RIVAL_ARMS_BASE` 0.4 + `RIVAL_ARMS_INDUSTRIAL` 0.35×fossil-lean(1−ARCHETYPE_GREEN_PROPENSITY) + `RIVAL_ARMS_MARKET` 0.25×(commerce/9)×(1−`worldArmsScarcity`)), where `worldArmsScarcity` = max(worldGoodScarcity steel, chemicals) — Liebig-max chokepoint mirroring the player's `armamentsCapacity()` min; pure core `rivalArmsCapacityCore` unit-tested exactly. `rivalWarWinChance(r,a,b)` (diplomacy.ts, used by `endForeignWar`) weights each side's pop by the SAME `FLOOR+(1−FLOOR)×cap` equip curve as `warPower()`. **Flag-gated on `rivalClimateResponse`: capacity=1 flag-off → ×1.0 equip cancels IEEE-exactly → default sweep BYTE-IDENTICAL (diffed + skeptic A/B-worktree-proven). Same single rng draw → the flagged climate sweep re-baselines BOUNDEDLY: 5/8 seeds stayed byte-identical, 3 (1021/1028/1049) diverged only where a tilted peace flipped a winner.** (2) **210f311 wartime materiel draw** — `consumeWarMateriel(r)` (military.ts, called beside `consumeWarSupply` in the monthly block): fielded unit power × `WAR_MATERIEL_PER_POWER` 0.01 units of EACH of steel+chemicals drawn from real town `goodStocks` via `drawGood` (its FIRST live caller); shortfall saps morale by unmet fraction (≤`WAR_MATERIEL_MORALE_DRAG` 8/mo, floor 30 = the supply-crisis floor) which `warPower()` reads → the loop closes; untracked ledger (pre-industrial war) skipped — never a conjured penalty. playerWar-gated → default AND climate sweeps BYTE-IDENTICAL pre→post this commit. Skeptic probe proved the tick wiring live: forced 1500-militia war, steel 50.85→5.85 over 3 sim-months = exactly the 45-unit draw. Draw magnitude is material (probe: national stocks ~700 accumulate ~1.4/mo by 1962 → a 200-militia army flips them into decline). (3) **7c70d83 Phase D space wonders** (worktree agent, pure data+tests, cherry-picked) — techtree +`rocketry`(1955, aviation+chemical_industry) +`satellites`(1965) → 56→58 nodes (count test bumped); region_buildings +3 uniques passing every wonders.test invariant: `space_program`(420, info +0.10, prestige 40), `satellite_network`(450, all +0.06, prestige 45), `orbital_station`(560, industry +0.15, prestige 60, era-2090 photo-finish capstone); probe: rivals claimed 3/3 in both probe seeds (space_program ~1966!). **HEALTHY default re-baseline** (rival wonder race shifts picks post-1955): default sweep only outcome flip = seed 1000 dystopia→drowned, wars still 0/run; NEW PINNED BASELINES — default 3 drowned/5 dystopia; climate sweep wars 21–52 (was 22–65), 5 drowned/3 dystopia. **KEY VERIFY FINDING (minor, the one to remember):** under `rivalClimateResponse` ALONE the scarcity CHOKE is inert — legacy `worldGoodScarcity` is structurally pinned at 0 (stocks dwarf demand), so wars are archetype/commerce-decided only; the choke goes LIVE with `SIM_CONSUMER_DEMAND=1` too (same latent dependency as the pre-existing `rivalResourceGrievance`). Measured combined climate+consumer sweep: **wMkt 21.5–27.2% (world market genuinely tight), wars 19–53, 3 drowned/5 dystopia** — the full scarcity-decided regime, now pinned. **GOTCHAS:** (a) vitest crawls `.claude/worktrees/` — ALWAYS `--exclude '**/.claude/**'` on full runs when agent worktrees exist. (b) bench under concurrent sweeps false-FAILs (24.6ms vs 16.7 gate) — run it SOLO (true worst this session 11.2–13.9ms). (c) new tests call `consumeWarMateriel`/`rivalWarWinChance` directly — deleting a call site would stay green (skeptics empirically verified the wiring live; coverage debt noted, not a bug). (d) skeptic-found PRE-EXISTING first-save key-order wobble on a mid-war save (zoningMix/playerWar key order; value-lossless, 2nd round-trip byte-stable; reproduced at 5c946cf) — not session 15, don't chase it as a regression. (e) `npm install --ignore-scripts` still required (electron postinstall 403). **NEXT:** (i) surface `rivalArmsCapacity` in the diplomacy panel (intel-gated display like agendas — render-only, byte-identical); (ii) extend the arms base into `rivalWarPower()` so embargoes bite PLAYER-vs-rival wars too (live-play-only balance change — design call on gating); (iii) inc 4: size-scaling govt outlays (SPIRAL CONSTRAINT — surplus-aware); (iv) re-evaluate the session-13 default-flip item with the NEW combined sweep (wars+arms+wonders now diversify outcomes: is 8/8-drowned still the blocker?); (v) Phase C multi-continent remains the big rock. Human-gated: climate-threshold tuning + any default-flip.

**Session date:** 2026-07-01 (session 14) · **Repo:** aslater100/Centuria · **Branch:** claude/parallel-sessions-workflow-g9zych · **Tests:** 1365 passing (was 1356) · tsc green · serialize-determinism BYTE-IDENTICAL · save-size green · bench PASS (worst 12.7ms) · **THIS SESSION — THE ARMAMENTS CHAIN: military strength is forged from industry (economy-realism arc, inc 2; 1 feature commit + handoff).** User: *"continue the work, run multiple parallel sessions… deeply. agentic. accurate. Compress often. Avoid token bloat. Create a loop … less is more but make that distinction."* **Loop I ran:** understand-Workflow (3 parallel Explore scouts → compressed line-anchored maps of the military + goods + harness seams, so the main context never had to hold 13k lines of region.ts) → design → implement → gate (tsc/tests/determinism/save-size/bench/2 sweeps) → verify-Workflow (3 adversarial skeptics) → handoff. **KEY DIAGNOSIS (a probe, not a guess):** the live military isn't where the inc-2 spec assumed. (1) The rival↔rival wars session 13 lit up (`startForeignWar`, region.ts:10401 — the 22–65/run measured in the climate sweep) are **abstract**: they push a random-duration entry + bump the export `warBoom ×1.5` and resolve with NO combat power. (2) `equipmentLevel`/`computeCombatPower`/`ArmyGroup` are **Phase-16 scaffold** (`systems/military.ts` + `tests/phase16.test.ts`) — `computeCombatPower` has NO live caller in region.ts; the live player-war combat fn is the unit-based `warPower()`, which read no equipment. (3) The autoplay player never proclaims a nation → never holds `playerWar` → every military path is dormant in every headless sweep (that's WHY the sweeps can stay byte-identical). **KEY DESIGN CALL — the byte-identical trap:** the spec's "add an `armaments` intermediate good" is NOT byte-identical — a new `INTERMEDIATE_GOODS` entry folds its cascade level into `result.health` → `supplyChainHealth` (a **serialized** field feeding `industryOutputMult`→GDP), shifting the default sweep even with a production gate; and a terminal arms good has 0 downstream demand so `worldGoodScarcity('armaments')` is pinned at 0 anyway. So I **derived the arms base from the EXISTING steel+chemicals cascade levels** instead — a pure read, zero catalog/cascade perturbation, identical coupling. **SHIPPED (all in region.ts):** `armamentsCapacity()` = `clamp(min(goodLevels.get('steel'), goodLevels.get('chemicals')), 0, 1)`, returns **1** if either input is undefined (pre-1920 / a fixture that never ran the goods tick — never invents a penalty); consts `ARMAMENTS_WARPOWER_FLOOR = 0.5` + `ARMAMENTS_STRAIN_PREMIUM = 1.0` (placed after `BLOCKADE_UPKEEP_PER_POP`, region.ts:2030); **three player-war-gated couplings** — (i) `warPower()` return × `equip = w ? FLOOR + (1−FLOOR)·capacity : 1` (gated on `w=this.playerWar` so the no-war population fallback is untouched; floored at 0.5×, never zeroed; scales OUR power only, allies bring their own arms); (ii) `recruitUnits()` `totalCost = Math.round(count·recruitCost·(1 + (1−capacity)·PREMIUM))` — ×1 at a full base so no existing recruit test moves; (iii) the monthly war-upkeep term `pop·warMob.upkeepPerPop·(1 + (1−capacity)·PREMIUM)` (region.ts:6250). **The point:** controlling coal/iron/chemicals (the "power/resources" the user wants wars fought over) now materially decides whether a belligerent can field, afford, and sustain a military — an embargo bites the army, not just civilian goods. **BYTE-IDENTICAL to every sweep** (couplings reached only via `playerWar`/`warMob`, which autoplay never sets; no new good, no new serialized field): default + `SIM_RIVAL_CLIMATE_RESPONSE=1` 8-seed×181y sweeps diffed **BYTE-FOR-BYTE identical** pre→post. Gates: tsc clean; **1365 tests** (1356 + 9 `tests/armaments-chain.test.ts`: capacity Liebig-min/clamp/undefined-fallback; warPower strained<full + collapse=FLOOR×full + no-war-untouched; recruit premium curve + full-base-exact); serialize-determinism BYTE-IDENTICAL; save-size green; bench PASS (worst 12.7ms); verify-Workflow **3/3 refuted=false, 0 findings**. **DECISIONS (the "make the distinction"):** did NOT add an `armaments` catalog good (supplyChainHealth perturbation + no downstream demand → pure-read wins on safety AND equivalence); did NOT touch the Phase-16 ArmyGroup/`computeCombatPower` scaffold (not the live path) — wired the live `warPower()`; did NOT make the abstract rival↔rival wars combat-resolved (rivals are off-map abstractions with no industry — a multi-session lift, deferred). **GOTCHAS:** (a) `npm install` still aborts on the electron postinstall 403 → `npm install --ignore-scripts`. (b) adding to `INTERMEDIATE_GOODS` is a **re-baseline**, never byte-identical, because `supplyChainHealth = mean(cascade levels over ALL active goods)` is serialized and feeds GDP — reach for a pure read of `goodLevels` when byte-identical is required. (c) `goodLevels` is a transient per-tick `Map` (region.ts:3486 `new Map()`, reassigned in `tickIntermediateGoods`), NOT serialized; `.get()` on the empty map returns `undefined`, not a throw. (d) tsc doesn't typecheck `tests/`. **NEXT:** rival-side arms base (make the abstract rival↔rival wars scarcity-decided → the piece that makes this MEASURABLE in the climate sweep); a physical wartime steel/chemicals draw (the full military-industrial loop, still player-war-gated); inc 3 size-scaling govt outlays (SPIRAL CONSTRAINT — surplus-aware); Phase D space Wonders (additive, low-risk). Human-gated: climate-threshold tuning + default-flip variety counterweight.

**Session date:** 2026-07-01 (session 13) · **Repo:** aslater100/Centuria · **Branch:** claude/parallel-sessions-workflow-7n4ms6 · **Tests:** 1356 passing (was 1327) · tsc green · serialize-determinism BYTE-IDENTICAL · save-size green · bench PASS (worst 12.5ms) · **THIS SESSION, PART 1 — THE CENTURY BECOMES LEGIBLE: a render-only Century Graph (commit 1).** User: *"continue the work, run multiple parallel sessions… deeply. agentic. accurate. Compress often. less is more but make that distinction."* Approach: 2 parallel read-only Explore scouts mapped (a) the charter `requiresNation` faithfulness item and (b) the UI stats/graph seams; I shipped ONE safe high-value UI win and deliberately skipped the rest (the "less is more" call), then documented the blocked headline item. **SHIPPED — CENTURY GRAPH.** The sim has recorded `statsHistory` (annual GDP/pop/warming/treasury/satisfaction, one snapshot each January, `STATS_HISTORY_MAX` 200, ALREADY serialized at region.ts:11441) for many sessions but the UI never drew it → the century of economy sim was invisible. New `src/ui/centuryGraph.ts`: a PURE unit-tested geometry core `graphPoints(values,w,h,pad)` (per-series min/max normalize, higher=higher on screen, robust to empty→''/single/flat → level line, never /0) + `centuryGraphHtml(history)` rendering 4 independent mini line-charts with compact K/M/B + £/+°C readouts, empty history → '' so callers concat unconditionally. Wired the SAME function into TWO sites: (1) 2100 **Century Report modal** (`drawCenturyReport`, regionview.ts ~L2626, after grades / before dynasty) and (2) **live Economy [E] overview** (`economyPanelHtml` ~L5197, right after the monthly `sparklineGrid`) — so THE LONG VIEW · 1919–<year> is visible ANY time, not just at 2100. CSS `.cv-century-*` in style.css mirrors `.cv-sparkline*`. **BYTE-IDENTICAL to the sim** (reads only already-serialized `statsHistory`; no sim/serialize/tick change, no new field, no RNG). Gates: tsc clean, **1337 tests** (1327+10 `tests/century-graph.test.ts`: empty/single/flat/rising/width-span geometry + no-NaN + 4-cell render + compact formatting), determinism + save-size + bench unaffected. **VERIFIED VISUALLY** (no full browser app here): `preview-century-graph.ts` runs a REAL 181-year seed-1000 run, feeds `r.statsHistory` to `centuryGraphHtml`, writes standalone HTML with the CSS → screenshotted via Playwright → four clean trend lines (GDP £20.8M, pop 75.5K, warming +2.6°C, treasury boom/bust). **DECISIONS (the "make the distinction" part):** (i) SKIPPED faithfulness #3 — "enforce `requiresNation`" is NOT byte-identical: autoplay charters `central_bank_charter` at STATE tier (never reaches nation), so the nation gate would break the session-11 cost-push chain + `tests/autoplay-statehood.test.ts:47` (asserts the bank IS chartered). Low value + hidden trap. (ii) DEFAULT-FLIP #2 measured but NOT shipped: both-flags-on 8-seed×181y = **8/8 DROWNED**, infl 4.3–5.4%, treas/GDP ~20mo (session-12 sink holds), pop 68–88k, fShort 15–24%, wMkt 3.6–5.5%. Blocker is a balance-FEEL call (8/8 drowned = monotony), needs a `consumerDemand`-gated variety counterweight (e.g. development-scaled green boost) + human playtest BEFORE the flip — documented in HANDOFF top block + backlog. **GOTCHAS:** (a) install: electron postinstall (binary download) fails on the network policy and ABORTS the whole `npm install` before linking `.bin/` → tsx/vitest missing → use `npm install --ignore-scripts` (electron/playwright binaries aren't needed for tests/tsc/sim). (b) Playwright screenshot from a scratchpad `.mjs`: ESM resolves bare `playwright` from the script's dir, not cwd → run the `.mjs` from inside the repo (so node_modules resolves) and pass `executablePath: '/opt/pw-browsers/chromium-1194/chrome-linux/chrome'` (the pinned build; PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers). (c) `tsc --noEmit` does NOT typecheck `tests/` (standing gotcha) — run new/changed test files. **NEXT:** the default-flip variety counterweight (see #2); INC 2 military-industrial complex (session-12 plan below); more render-only UI legibility (live-stats skyline backdrop tint; decompose tooltips).

**THIS SESSION, PART 2 — EXISTENTIAL CLIMATE RESPONSE: the world fights back (commit 2).** User (mid-session follow-up): *"the military increment, wars and military should show up more often. these nations are too comfortable with the drowned state. I would expect to see nations or factions rising to save themselves from that fate, diplomatically, economically, and or militarily. nations should be fighting for things like land, water, power. grow the world into a multi continental landscape, complete with complex environs on land and sea and a way to sim the space environ (without needing to render space itself, but the satellites etc.). plan how this would be done first then implement."* **Approach — used the harness's own EnterPlanMode/ExitPlanMode flow** (the request is a genuine architectural expansion, not a slice): 3 parallel read-only Explore scouts mapped world geography, war-frequency/AI motivation, and space/tech hooks; wrote a plan file (`/root/.claude/plans/async-tumbling-crystal.md`) scoping a 4-phase roadmap (A=existential response, B=military-industrial+power good, C=multi-continental world, D=space Wonders), got it approved via `ExitPlanMode`, then implemented Phase A only. **KEY DIAGNOSIS (a probe, not a guess): the war-frequency knobs (`ARCHETYPE_WAR_FREQ_MULT`) were measured firing on NOTHING** — an 8-seed×181y default sweep showed **0 wars, every seed**. Root cause found via a manual-tick probe: rival relations-drift settles at a comfortable equilibrium (Hegemon baseline ≈ −12 to −37) that never crosses the −40/−60 hostility gates that unlock mischief/war — so my FIRST attempt (a multiplier on the existing war-roll alone) changed nothing, because the roll was never even being attempted. **THE ACTUAL FIX needed two parts** (both `systems/diplomacy.ts`, new consts in `region.ts` beside `ARCHETYPE_WAR_FREQ_MULT`): a per-rival `rivalClimateUrgency(r,rv)` signal (pure, exported — `warmingC/WORLD_GREEN_URGENCY_C` scaled by `1−ARCHETYPE_GREEN_PROPENSITY`), then **(i) `URGENCY_RELATIONS_DRAG` (45)** subtracts from the relations-drift BASELINE (not just the roll) — the relationship curdles as warming worsens — applied in BOTH the rival→player drift AND the rival↔rival drift in `tickForeignRelations`; **(ii) `URGENCY_AGGRESSION_LIFT` (1.5)** then multiplies the war-roll chance once hostile, on BOTH the player-war roll AND the rival↔rival `startForeignWar` roll (the latter needs no `nationProclaimed` — critical, since the autoplay player never proclaims one, so it's what makes wars measurable in the default sweep at all). **A REAL `resource_dispute` casus belli**: found a **dead `resource_denial` CB scaffold** already in the codebase (`CBType`/`generateCasusBelli`, region.ts:10551) — tested only by unit tests, never wired into the live war paths, and structurally broken even if wired (`settlements.filter(s => s.factionId === rivalId)` — but a `RivalNation`'s id-space is disjoint from `Settlement.factionId`, the exact leg-2 id-space split HANDOFF already names as the multi-session rock). Instead added `resource_dispute` to the LIVE `CasusBelli` union with a new `rivalResourceGrievance(rv)` reading signals that are REAL for an off-map RivalNation: population scale (`RESOURCE_DISPUTE_POP_THRESHOLD` 15,000, mirrors the existing `RIVAL_WORLD_POP_NORM` scale from goods.ts) AND genuine `worldGoodScarcity` (`RESOURCE_DISPUTE_SCARCITY_THRESHOLD` 0.35) — wired into `availableCasusBelli` + the rival-initiated path (previously hardcoded to always cite `'fabricated'`/`'border_dispute'` regardless of the real motive). **Green-leaning self-preservation:** new `RivalClimateBloc` type (mirrors `RivalTradeBloc` field-for-field) + `tickRivalClimateBlocActivity` (new diplomacy.ts function, same gate→join-or-found shape as `tickRivalTradeBlocActivity`) lets high-propensity rivals (`CLIMATE_BLOC_GREEN_THRESHOLD` 0.6) with non-hostile relations found/join a coalition; **had to tune the relations bar down to `CLIMATE_BLOC_RELATIONS_THRESHOLD = −15`** after a probe showed even two green-eligible archetypes can sit at a merely-neutral ~−6 baseline on personality/bloc terms alone (the trade-bloc's +40 bar would never be met). Membership boosts `archetypeGreenShare()` by `CLIMATE_BLOC_GREEN_BOOST` (0.25) — directly bends `worldEmissions`/the era verdict, so a coalition is a REAL alternative to war, not flavor text. **New telemetry:** `warsDeclaredCount` (lifetime counter, region.ts, incremented in `startPlayerWar`+`startForeignWar`, NOT serialized) + a `wars` headless column — this is what let me literally SEE the 0-wars baseline and confirm the fix. **MEASURED (8-seed×181y, `SIM_RIVAL_CLIMATE_RESPONSE=1`):** wars **0 → 22–65/run**; outcome split **6 dystopia/2 drowned → 4/4** on the identical 8 seeds (genuinely more variety, not less — conflict feeds `gpP%`/great-power-pressure telemetry up 18–29% via the existing war-import channel, while the coalition mechanic pulls some seeds toward less-drowned outcomes); economy stays bounded/solvent throughout (treas/GDP 0.2–5.1mo, all solvent). **BYTE-IDENTICAL when off** (every new draw/probability behind `if (r.rivalClimateResponse)`; the 4 previously-tested default-sweep seeds matched byte-for-byte after ALL edits — treasury/GDP/pop/sat/pBld/flows/scarcity/wMkt/gpP identical to the pre-session capture). `rivalClimateBlocs` is a new serialized field (mirrors `rivalTradeBlocs`, `?? []` backfill) but stays `[]` whenever the flag has never fired → byte-identical in JSON terms too. Gates: tsc clean, **1356 tests** (1337+19 new `tests/rival-climate-response.test.ts`), determinism + save-size green, bench PASS (worst 12.5ms), vite build green. **GOTCHAS:** (i) the `worldSim` test fixture (borrowed from `tests/world-anchor.test.ts`) needs an explicit `Object.defineProperty(r,'year',{get:()=>2000})` override — `RegionSim.create()` starts at 1919 and `textiles` (the standard scarcity-test good) has `eraUnlock:1920`, so an un-overridden fixture silently measures 0 scarcity for EVERY good; cost me a full debug cycle. (ii) `RegionSim.deserialize` is a STATIC factory taking a JSON STRING (`RegionSim.deserialize(r.serialize())`), not an instance method — grep an existing serialize-round-trip test before guessing the API. (iii) don't test probability shifts by sampling RNG outcomes when a DETERMINISTIC formula-level test will do — I tested the relations-drag by calling `updateDiplomacy(r)` once with fixed `warmingC` and comparing the resulting `relations` flag-on-vs-off directly, no flakiness. **NEXT (own future sessions — full design in the plan file):** tune `URGENCY_RELATIONS_DRAG`/`_AGGRESSION_LIFT`/`CLIMATE_BLOC_*` after a human playtest; Phase B — military-industrial complex, extended to add a literal `electricity`/`fuel` good (makes "power" fightable-over via the SAME generic scarcity-CB, zero extra CB work); Phase C — multi-continental world (own multi-session arc, sequenced last: worldgen determinism, save-version migration for the baked-in 128×128 grid, O(N²) perf at scale); Phase D — space/satellite Wonders (rocketry→satellite tech ladder, 3 civ-wide Wonders on the exact existing Wonder pattern — lowest risk of the four, startable anytime).

**Session date:** 2026-07-01 (session 12) · **Repo:** aslater100/Centuria · **Branch:** claude/parallel-sessions-workflow-qg2jqc · **Tests:** 1326 passing (was 1324) · tsc green · serialize-determinism BYTE-IDENTICAL · save-size green · bench PASS (worst 12.1ms) · **THIS SESSION — THE STATE SPENDS: the autoplay state-budget SINK bounds the treasury (1 feature commit + handoff).** User: *"continue the work, run multiple parallel sessions... deeply. agentic. accurate. Compress often. Create a loop."* Approach: 2 parallel read-only Explore scouts mapped (a) the autoplay-statehood/budget wiring + funding seam, (b) the headless harness/telemetry/gate commands; then implemented the session-11 top rock. **THE PROBLEM (reproduced):** `SIM_AUTOPLAY_STATEHOOD=1 SIM_CONSUMER_DEMAND=1 npm run sim -- 181 8` → treasury £7.4–9.9bn, headless treas/GDP(mo) **35.9–41.1** (vs 0.2–5.6 default-OFF). A statehood probe showed the autoplayer proclaims + charters a bank by ~1960 (state=true, bank=true, infl 4.3–5.5) but then income-tax revenue (+3% GDP/mo) outruns its slow purse-gated dev spend → the true ratio treasury/`gdpLastMonth` CLIMBS 0.27→2.98 over 2000–2100 and keeps rising. **THE FIX — a government-consumption sink in `monthlyEconomy` (`region.ts`, right after the `this.treasury +=` line ~6150).** `if (this.autoplayStatehood && this.stateProclaimed) { treasury -= max(0, treasury − gdpLastMonth×AUTOPLAY_STATE_RESERVE_MONTHS) × AUTOPLAY_STATE_GOV_SKIM }` — new consts `AUTOPLAY_STATE_RESERVE_MONTHS = 1.5` (mirrors `RIVAL_RESERVE_MONTHS`) + `AUTOPLAY_STATE_GOV_SKIM = 0.5`, placed by the rival budget consts (~L757). Models the government consumption a real state runs (transfers/procurement/unmodeled services). Clamped ≥0 so it can NEVER drive the treasury negative → no false service-cut at the deficit branch (region.ts:6161). Converges to a bounded steady state `reserve + net-inflow/skim`; both terms scale with GDP so the bound holds in months-of-GDP terms forever (not a fixed £ cap). **KEY SCALE GOTCHA (cost me one iteration):** `gdpLastMonth` IS monthly GDP; the headless `treas/GDP(mo)` column divides `monthlyHistory.gdp` (a slightly different, un-multiplied monthly figure) by 12, so its "40 months" ≈ treasury/`gdpLastMonth` × 12 ≈ 3.3× monthly GDP. My first try used `RESERVE_MONTHS=3` → reserve `gdpLastMonth×3` ≈ treasury → surplus≈0 → drain never fired (output barely moved). Dropped to 1.5 → bites. **MEASURED (8-seed×181y, both flags on):** headless treas/GDP(mo) **40→~20** and now STABLE (probe: treasury/`gdpLastMonth` pins at ~1.52 from 2060 on, vs the no-drain 1.83→2.38→2.98 climb); infl% 4.3–5.5, pop, and the 8/8-drowned outcome split all UNCHANGED (the sink only touches the treasury). **BYTE-IDENTICAL when the flag is off:** default `npm run sim -- 181 8` matches the session-11 baseline VERBATIM (70108/37701/844486/19339/36531/527756/225977/639116); full determinism + save-size green. **TESTS:** +2 in `tests/autoplay-statehood.test.ts` (7→9): the sink pulls a flushed 40-mo treasury back under 3× `gdpLastMonth` in ~a year of ticks; the OFF guard leaves a flush COLONY hoard (>10× GDP) untouched. **GOTCHAS:** (i) `ELECTRON_SKIP_BINARY_DOWNLOAD=1 PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm install --ignore-scripts --no-audit --no-fund`. (ii) don't fold the drain into the `spending` var — that feeds the deficit/service-cut branch; subtract it as a separate CLAMPED outflow after the treasury update. (iii) don't manually flip `stateProclaimed` in a test then keep ticking — `completeIncorporation` sets much more (govLean, cohort conversion); the OFF test just runs a colony (flag off → guard false on both counts). (iv) `gdpLastMonth` ≠ `monthlyHistory.gdp` — see the scale gotcha above. **COMMIT 2 — EFFECT-SCALED BUILDING COSTS (economy-realism increment 1; `region.ts` + `tests/region.test.ts`; re-baseline).** User steer this session: *"treasuries running unbounded should highlight overly cheap national expenditures. military expansion should have its own manufacturing base, military industrial complex, base costs… extend this logic across other areas of the economy and govt as an option for a large growing nation. buildings should be expensive and scale with their effects."* 3 parallel read-only scouts mapped the building-cost / military-goods / government-spending surfaces. Shipped the clearest self-contained piece first: build/district cost is now `base × devFactor × effectCostMult(magnitude)`. New PURE public helpers on RegionSim (also for UI "why so costly" tooltips): `buildingEffectMagnitude(def)` = `|bonus|×span + research×1 + sat×0.02 + sight×0.02 + empireBonus×empSpan×4`, where `span = sector==='all' ? 3.5 : 1` (an all-sector bonus lands on ~3–4 sectors) and `empSpan` likewise for the empire sector; `districtEffectMagnitude(def)` = `|bonus| + DISTRICT_ZONE_BONUS×DISTRICT_ZONE_CAP` (flat + adjacency stacking); `effectCostMult(mag)` = `clamp(mag/EFFECT_COST_BASELINE 0.16, EFFECT_COST_MULT_MIN 0.7, EFFECT_COST_MULT_MAX 3.0)`. All 4 debit paths (player build/zone, rival build/zone) already route through `cityBuildCost`/`districtCost`, so human + AI pay the same effect-scaled price with no debit-seam rewrite. New costs @devFactor 1: waterworks 56, hospital 101, factory 156, power_station 263, university 337, data_exchange 375, wonders 840–1500 (were flat 60/90/100/120/120/150/280–500). Consts beside the AUTOPLAY_STATE_* block (region.ts ~L772). **Re-baseline (deliberate, on for everyone — matches the industry-brownout/terrain-yields precedent):** 8-seed×181y default sweep stays bounded/solvent (treas/GDP 0.1–6.9mo), pop 62–79k, player still builds (pBld 2–15 — pricier works did NOT starve development), outcome mix shifts toward dystopia (AI trajectories change). Flagged-on (autoplay sink + effect costs) still bounds ~20mo. Gates: tsc clean, **1327 tests** (region.test.ts: 2 hard-coded build-cost asserts re-baselined — the `cost==base@devFactor1` and the `treasury 500-60` ones — + a NEW effect-scaling invariant test: higher-effect building costs strictly more, mult stays in [0.7,3.0]), determinism + save-size green, bench PASS (worst 11.2ms). **GOTCHA:** two tests hard-coded raw build costs (`cityBuildCost(factory).toBe(factory.cost)`, `treasury.toBe(440)`); grep for literal cost numbers when re-baselining a cost formula. Districts unchanged in JSON but cost more via the mult.

**★ THE ARC — "ECONOMY SPENDING REALISM" (the user's vision).** The ballooning treasury is the SYMPTOM; cheap national expenditure is the disease. Root cause (gov-spending scout): revenue bonuses (income_tax +3%, progressive_tax +2%, central_banking +1%, carbon_levy +1%) are hard-wired %-of-GDP and STACK → +2–4%GDP/mo net surplus at developed scale, while NO spending sink grows with nation size. **INC 1 = buildings ✓ (this commit).** Remaining, each its own focused session:
- **INC 2 — MILITARY-INDUSTRIAL COMPLEX (the user's headline; additive, biggest lift).** Today there's NO armaments/weapons good; `equipmentLevel` inits 50 and decays on supply but has NO production source; militia is a one-time `flatCost(250)` + a GDP-share wage in publicSector; army units are flat `UNIT_TYPES.recruitCost` 10–80. PLAN (from the military scout): add an `armaments` intermediate good (inputs steel+chemicals, industry-attributed, ~1920 era unlock, baseOutput ~8) to the goods catalog (region.ts INTERMEDIATE_GOODS ~L268, systems/goods.ts production); make `recruitUnit` (region.ts:10359) + mobilization (MOBILIZATION_DEFS region.ts:1949, systems/military.ts tickMobilization) DRAW armaments — a shortage fails the recruit or charges a treasury premium; couple `equipmentLevel`/`computeCombatPower` (region.ts:10617) to the armaments base so a nation that can't out-PRODUCE can't out-FIGHT; add real base costs + upkeep. Embargo chains fall out (iron→steel→armaments). Tests: phase16, conquest, a new armaments-supply test. Own session — new-subsystem scope.
- **INC 3 — SIZE-SCALING GOVERNMENT OUTLAYS ("extend across govt as an option for a large growing nation").** A large developed nation runs a bigger state apparatus. ⚠️ **SPIRAL CONSTRAINT:** the handoff warns a steeper GDP-proportional public-sector lift (devShare ≥0.35) tips the info-era budget into a DEFICIT DEATH-SPIRAL (services auto-cut→satisfaction→emigration). Two safe forms: (a) a SURPLUS-AWARE, `has('income_tax')`-GATED drain — clamped ≥ a generous ~6-mo reserve so it can NEVER cause a service cut, and byte-identical for the no-income-tax default sweep (which is why the autoplay sink had to be autoplay-only: the default player has no income tax); or (b) a size-gated (`totalPop`/town-count ramp) ONGOING cost sized ≤ the observed +2–4%GDP surplus. Note effect is largely UNMEASURABLE in today's harness (default autoplay has no income tax; flagged-on is already bounded by the 1.5-mo sink) → it's forward realism for large HUMAN states; verify via a constructed large-nation fixture, not the sweep. Composes with the autoplay sink (min-reserve wins).
- **ALSO STILL LIVE (pre-arc):** flip `autoplayStatehood`+`consumerDemand` DEFAULT-ON (re-baseline: re-pin markers + the drowned-heavy split; both-on lands 8/8 drowned — decide if intended or wants a green counterweight); tune `AUTOPLAY_STATE_RESERVE_MONTHS`/`_GOV_SKIM`; the faithfulness/`requiresNation` cleanup; leg-2 id-space unification. **PRIOR session (11) below.**

---

**Session date:** 2026-07-01 (session 11) · **Repo:** aslater100/Centuria · **Branch:** claude/parallel-sessions-workflow-jeob23 · **Tests:** 1324 passing (was 1317) · tsc green · serialize-determinism BYTE-IDENTICAL · save-size green · **THIS SESSION — THE TOP ROCK FALLS: AUTOPLAY STATEHOOD lights up MEASURABLE cost-push inflation (2 commits + handoff).** User: *"continue the work, run multiple parallel sessions... deeply. agentic. accurate. Compress often. Create a loop."* Approach: 2 parallel read-only Explore scouts mapped (a) the arbitrage O(N²·G) scan + (b) the autoplay-statehood/central-bank gating chain; both items live in DIFFERENT files (`systems/arbitrage.ts` vs `region.ts`/`rival-ai.ts`) so they shipped as 2 clean independent commits. **(1) ARBITRAGE PERF (byte-identical, `systems/arbitrage.ts`).** The all-pairs price-arbitrage scan is O(N²·G) and each town pair called `computeCongestionTariff` (an O(R) `r.routes.find()`), plus a per-in-transit-flow O(R) `.some()` stranding check — tens of thousands of route comparisons/tick late-game. Built a per-tick `Map<"min>max",Route>` index once at the top of `tickPriceArbitrage` (`buildRouteIndex`, first-wins so it exactly mirrors `.find()` — both push sites `buildLink`/`blazeTrail` dedupe via `routeBetween`, so ≤1 route/unordered pair), reused for BOTH the stranding check and the tariff lookup → each O(R) scan becomes O(1). `computeCongestionTariff` keeps its 3-arg form (optional `routeIndex` param + `.find()` fallback), so tests/external callers unchanged. Byte-identical: determinism + arbitrage-global + phase15 (tariff-value asserts) green. **(2) AUTOPLAY STATEHOOD DIRECTOR (the top rock; `region.ts` flag + `systems/rival-ai.ts`).** DIAGNOSED first: over a full century the autoplayer grows to 5 towns / ~250k pop but stays a COLONY forever — state=FALSE, researches NOTHING (`activeResearch` never set → `tickResearch` inert → no statecraft/income_tax), polCap=0, so `hasCentralBank()`=false → session-9/10 cost-push inflation stays dormant (infl% pinned 2.0). Root cause: incorporation/research/law-making are player-only UI actions with no autoplay driver. NEW `advanceAutoplayStatehood(r)` walks the natural CODE-ENFORCED gates via the same public methods the UI calls: **STATE** `completeIncorporation('The Frontier State','council')` the moment `updateCharter` sets `ceremonyPending`; **TECH** `startResearch` steering toward the spine `['common_law','free_press','labor_law','income_tax','universal_suffrage','statecraft']` else cheapest (`availableToResearch()` enforces prereqs/era/requiresState); **BANK** `enactLaw('central_bank_charter')` once a STATE has statecraft+income_tax and elections (gated on `universal_suffrage`, NOT nationhood — `checkElection`) have banked ≥50 polCap. **KEY UNLOCK:** the whole chain is reachable by a STATE with NO nation — `checkElection` needs only `stateProclaimed`+`universal_suffrage`, and `enactLaw` enforces only `requiresState` (the law's `requiresNation:true` flag is NOT enforced in code). **MEASURED (4-seed×181y, SIM_CONSUMER_DEMAND=1 SIM_AUTOPLAY_STATEHOOD=1):** reaches state+central bank by ~2009; infl% **4.3–5.5 (MOVED off the pinned 2.0 — the deliverable)**. **GATED behind a NEW run-mode flag `autoplayStatehood` (`SIM_AUTOPLAY_STATEHOOD=1`), default OFF, SEPARATE from autoDevelop/autoExpand** — because state-tier income-tax revenue (+3%GDP/mo) OUTRUNS the autoplayer's dev spend → treasury runs UNBOUNDED (~40 months of GDP, £8–9bn; GDP itself balloons to £2–3bn). Default OFF ⇒ live human play + the **default sweep** + determinism harness byte-identical; default-sweep numbers (pop 61–77k, treas/GDP 0.8–7.4mo, fShort 12.9–22.3%, infl 2.0) MATCH the session-10 baseline exactly. **TESTS:** +7 `tests/autoplay-statehood.test.ts` (charter-on-ceremony / gate-respect / legal-research-only / bank-on-capital / bank-withheld-when-short / ON→state+bank by 2019 / OFF→stays-colony). **GOTCHAS:** (i) `npm install --ignore-scripts` (electron postinstall fails in this env). (ii) `has(id)` = `researched.has(id)` (techs+civics); adding to `r.researched` in a test satisfies law prereqs. (iii) statehood perturbs autoplay RNG (completeIncorporation's landless-faction loop) but ONLY under the flag → default sweep + determinism unaffected; the determinism test compares same-seed runs so is immune regardless. (iv) `computeCongestionTariff` is NOT self-gated on a routeIndex — pass it explicitly at the call site. **WEAK AREAS / NEXT (in priority):** **(a) THE autoplay state-budget SINK is now the top rock** — a flush autoplay state must SPEND its income-tax revenue (dev/links/military/services from surplus, or a modeled gov-spending sink) so treas/GDP stays bounded; ONLY then can `autoplayStatehood` (and thus measurable inflation) go DEFAULT-ON in the sweep. Evidence: with the flag on treasury→£8–9bn, ~40mo GDP. (b) once (a) lands, flip `autoplayStatehood` + `consumerDemand` ON by default (re-baseline) and tune `FINAL_SHORTFALL_OUTPUT_DRAG`/`_INFLATION`. (c) OPTIONAL faithfulness: either enforce `requiresNation` in `enactLaw` and make autoplay proclaim a NATION properly (blocked today by the 50%-territory convention gate autoplay reaches only 15–27% of — needs conquest/territory work), or accept the state-charters-a-bank path and drop the law's `requiresNation` flag. (d) leg-2 `RivalNation`↔`RegionalFaction` id-space unification still the multi-session rock. **PRIOR session (10) below.**

---

**Session date:** 2026-07-01 (session 10) · **Repo:** aslater100/Centuria · **Branch:** claude/parallel-agentic-sessions-csr7wh · **Tests:** 1317 passing · tsc + vite build green · serialize-determinism BYTE-IDENTICAL · save-size green · bench PASS (worst 12.4ms) · **THIS SESSION — CONSUMER-DEMAND INCREMENT 3: THE HOUSEHOLD SHORTAGE BITES THE MACRO (the stagflation pair; 1 feature commit + handoff).** User: *"continue the work, run multiple parallel sessions... deeply. agentic. accurate. Compress often."* Approach: 3 parallel read-only Explore scouts mapped (a) the consumer-demand/output-drag code, (b) the faction fisc/statehood/inflation machinery, (c) the arbitrage O(N²·G) scan — then implemented the highest-value + byte-identical-safe item SEQUENTIALLY (all edits touch the region.ts economy core, so no parallel edits). Shipped the DEFERRED session-9 NEXT item (b): increment 2 made an unmet goods shortfall FELT in *satisfaction*; increment 3 makes it bite *output* and *prices* — the two halves of a consumer-goods stagflation, both riding the transient `finalConsumptionShortfall` (EXACTLY 0 when `consumerDemand` off → live human play byte-identical, no new RNG). **(1) OUTPUT HALF (UNgated, `updateSectors` region.ts:6723).** Industry output ×`(1 − localGoodsScarcity×0.10 − finalConsumptionShortfall×FINAL_SHORTFALL_OUTPUT_DRAG)`; new **`FINAL_SHORTFALL_OUTPUT_DRAG = 0.20`**, additive with the local-goods drag, bounded (worst-case combined 0.30 → industry ×0.70, never zeroed). **No spiral**: production `level` is RAW-supply-driven (`resolveSupplyChainGraded`/`rawSupplyLevel`), not output-driven, so dragging `s.output` can't feed back into goods stocks → `finalConsumptionShortfall`. **(2) PRICE HALF (cost-push, `tickMonetary` monetary.ts:52).** inflation target += `finalConsumptionShortfall × FINAL_SHORTFALL_INFLATION` (0.15), the same channel as the raw-cascade + local-goods pushes. **CALL-SITE gated** on a central bank (region.ts:5784 `if (this.hasCentralBank()) tickMonetary(this)`), so it bites a HUMAN / future-statehood nation in a downturn, NOT the current central-bank-less autoplayer → **forward-wiring: currently unreachable in any live run mode** (needs `consumerDemand` ON *and* a central bank to co-occur; today ON=headless-only, central-bank=humans-only), unit-tested rather than sweep-visible. **MEASURED (8-seed×181y, SIM_CONSUMER_DEMAND=1):** bounded re-baseline — pop 62–77k, ALL solvent (treas/GDP 0.2–7.4mo), fShort 13–22% (no runaway); the output drag shifts trajectories (several drowned→dystopia: less industry → less emissions → less warming, an emergent chain). infl% still 2.0 (the autoplayer has no central bank — see above). **OFF = byte-identical** (both terms multiply a signal that is 0 → `1 − a − 0 == 1 − a` and `+ 0` exactly; determinism + save-size green). **TESTS:** +4 in `tests/consumer-demand.test.ts` (now 25): output-drag magnitude (ratio == `1 − 0.5×DRAG`)/industry-only/inert-at-zero, cost-push under a bank (Δ == `0.5×INFL×0.15`), byte-identical guard. **GOTCHAS:** (i) `tickMonetary` is NOT self-gated on `hasCentralBank()` — the gate is the CALL SITE (region.ts:5784); calling `tickMonetary(r)` directly in a test runs it regardless (my first cost-push test wrongly assumed self-gating and failed). (ii) `npm install --ignore-scripts` (electron postinstall fails in this env). (iii) `updateSectors` is private → test via `(r as unknown as {updateSectors(t):void})`; two fresh same-seed sims are rng-identical so an injected `finalConsumptionShortfall` isolates the drag exactly. **WEAK AREAS / NEXT:** (a) **autoplay statehood is now the top rock** — until the autoplayer proclaims + charters a central bank, BOTH the cost-push inflation (this session) and the existing `localGoodsScarcity` cost-push stay dormant in the sweep; making inflation measurable needs it. (b) **flip `consumerDemand` ON by default** (re-baseline; wants a human downturn playtest) — this is what makes increments 2+3 live for humans and lights up the cost-push where a central bank exists. (c) tune `FINAL_SHORTFALL_OUTPUT_DRAG`/`_INFLATION` + `PLAYER_TOWN_CAP`/`FINAL_APPETITE`/`GOODS_SATISFACTION_PENALTY` after a human playtest. (d) arbitrage perf: the all-pairs scan is `systems/arbitrage.ts:134`; cheapest byte-identical win = a per-tick `Map<"a>b",Route>` route index to replace the O(R) `computeCongestionTariff` linear scan (arbitrage.ts:31) — no RNG reorder. (e) leg-2 `RivalNation`↔`RegionalFaction` id-space unification still the multi-session rock. **PRIOR session (9) below.**

---

**Session date:** 2026-07-01 (session 9) · **Repo:** aslater100/Centuria · **Branch:** claude/high-impact-agentic-2zpyji · **Tests:** 1313 passing · tsc + vite build green · serialize-determinism BYTE-IDENTICAL · save-size green · bench PASS · **THIS SESSION — THE ON-MAP ECONOMY COMES ALIVE (3 commits, two run-mode flags default OFF → live human play byte-identical; the headless sweep turns them on).** User: *"continue the work, deeply. agentic. accurate. work on the biggest lift, most impact"* + explicitly *"let the auto player found more towns?"*. The headline arc ("make the world GLOBAL") had reached signal-only (increment 1: the world market can TIGHTEN, but the on-map economy stayed inert — "telemetry-only"). This PR makes it FELT. **(1) AUTOPLAY PLAYER AUTO-EXPANDS (`autoExpandPlayer`, new flag, default OFF, headless ON).** The autoplay player held ONE town all century (rivals expand, the player was skipped by `updateRivalAI`), so cross-town specialisation/arbitrage/the goods coupling were under-exercised and its satisfaction crashed to 0 by 2100 (one town, 21k pop → chronic crowding). Extracted the rival expansion block (was inline in `updateFactionAI` ~L12163) into a shared purse-seamed **`maybeExpandFaction(faction, knobs, factionPop)`** (`factionDevPurse`/`spendFactionDev` seam → rival path BYTE-IDENTICAL: reads/writes `faction.treasury` in the same order, `aiRng.chance(expandChance)` in the same short-circuit slot); the player branch in `systems/rival-ai.ts` now calls it (gated on `autoExpandPlayer && playerMayExpand(faction)`) then develops. New public `factionPopulation`/`playerMayExpand`; new **`PLAYER_TOWN_CAP = 5`** (well under the rival `settlementCap` 12 — a sprawling 12-town player leaves every town under-built → low output → near-zero treasury; 5 stays well-developed). **(2) CONSUMER-DEMAND INCREMENT 2 — the per-town final-consumption SINK (`consumerDemand`, existing flag).** The deferred increment: `localFinalGoodDemand(r,t,goodId)` = a town's household appetite = its POPULATION share of the world's exogenous final appetite (`finalGoodDemand`); Σ over towns == the world figure (mirrors how `distributeGoodProduction` splits production by sector share). The **SINK**: `tickIntermediateGoods` now DRAINS each town's stock by that appetite every month (after production). A town that PRODUCES a good feeds its people (stays flush → base price → exports); one that CONSUMES but doesn't make it runs its shelf to 0 → `localGoodPrice` rises → **arbitrage ships it in → the on-map economy RESPONDS**. `localGoodPrice` folds the final demand into its scarcity (the input-only demand missed all 8 terminal goods). New **TRANSIENT** signals (NOT serialized → save format byte-identical to HEAD): per-town `goodsShortfall` map (∈[0,1]), world-aggregate `finalConsumptionShortfall`, + a `worldPopCache` so the O(N²·G) arbitrage price scan doesn't re-sum cohorts per call. **(3) THE ECONOMY IS FELT — goods shortfall drags satisfaction.** `dailyUpdate`'s satisfaction `target` gains `-goodsShortfall(t) × GOODS_SATISFACTION_PENALTY` (12), per-town, through the existing 0.08/day EMA, gated on `consumerDemand`. **MEASURED (8-seed×181y):** OFF (default/live) = BYTE-IDENTICAL to the pre-consumer-demand baseline (flows/lScar/fShort all 0; determinism + save-size green). ON (`SIM_CONSUMER_DEMAND=1`, multi-town via autoExpand): **flows 8–52 active shipments (was a structural 0 — real on-map goods now MOVE)**, final-consumption shortfall 15–22%, wMkt 2.4–6.0%, pop 52–84k, all bounded/solvent, outcomes 4 dystopia/4 drowned. New headless columns: `pTwn`, `flows`, `lScar%`, `fShort%`. **TESTS:** re-baselined the increment-1 "byte-identical on-vs-off" test to assert the intended DIVERGENCE + determinism + bounds; +6 new sink/coupling tests in `tests/consumer-demand.test.ts` (now 21): pop-share math, price stock-sensitivity, drain, producer-vs-consumer shortfall, the FELT satisfaction coupling (ON drags, OFF inert). **GOTCHAS:** (i) `npm ci`/`npm install` fails on the electron postinstall in this env → `npm install --ignore-scripts` (need tsx/vitest/vite/tsc only). (ii) a single `r.tick()` may advance 0 days (sub-day calendar) → to test a daily coupling, loop until `r.day` changes, re-injecting the transient each sub-tick (it's before the goods tick that overwrites it). (iii) `finalGoodDemand`/`localFinalGoodDemand` short-circuit to 0 when `consumerDemand` off (BEFORE `worldPopulation`) → zero live-play cost; the sink is skipped entirely off. **WEAK AREAS for next session:** (i) **Multi-town autoplay treasury runs LEANER** than the single-town hoard (0.2–5.6 mo of GDP, seed-dependent, ALL solvent — a growing nation reinvests) — root cause: the autoplay player NEVER proclaims statehood (`nationProclaimed=false` all game) so it runs a primitive colony fisc across 5 towns; a real fix is letting the autoplay player proclaim/incorporate (bigger re-baseline). (ii) **Inflation still pinned 2.0% even ON** — `tickMonetary`'s cost-push (the `localGoodsScarcity × LOCAL_GOODS_INFLATION` channel) only runs `if (hasCentralBank())`, which the autoplay player never has → cost-push can't bite; the industry output-drag (`LOCAL_GOODS_OUTPUT_DRAG`, ungated) DOES apply but `localGoodsScarcity` stays 0 (see iii). (iii) **`localGoodsScarcity` (input-stranding gate index) stays 0 even ON** — the sink drives FINAL-consumption shortage (fShort 15–22%), not production-input starvation (producers keep enough inputs), so the input-stranding coupling stays dormant; wiring `finalConsumptionShortfall` into the output-drag/cost-push (ungated) would make the macro bite — a deliberate re-baseline, deferred. (iv) **Arbitrage O(N²·G) price scan** is a pre-existing worst-tick cost at high town counts (~20–47ms at 38 towns, present OFF too — NOT introduced here; the `worldPopCache` trims my added part); a spatial/route index or a per-tick opportunity cap would fix it. (v) leg-2 `RivalNation`↔`RegionalFaction` id-space unification still the multi-session rock. **NEXT:** (a) let the autoplay player PROCLAIM statehood so its fisc/inflation machinery engages (fixes weak-areas i+ii, measurable inflation); (b) feed `finalConsumptionShortfall` into the ungated macro coupling (output-drag) + consider flipping `consumerDemand` ON by default (re-baseline, wants a human playtest); (c) tune `PLAYER_TOWN_CAP`/`FINAL_APPETITE`/`GOODS_SATISFACTION_PENALTY` after a human playtest; (d) the arbitrage perf index. **PRIOR session (8) below.**

---

**Session date:** 2026-07-01 (session 8) · **Repo:** aslater100/Centuria · **Branch:** claude/high-impact-agentic-88859p · **Tests:** 1307 passing · serialize-determinism green · tsc + vite build green · bench-region PASS · **THIS SESSION: Track-C decomposition waves 3+4 — 23 tick methods → 18 new `systems/*` modules. region.ts 13,985 → 13,321 (−664 lines), ALL BYTE-IDENTICAL, one commit per extraction. PR #325 (draft, CI green on wave-3 push).** Wave 3 cleared the ENTIRE pre-analyzed backlog (14 methods incl. the `runElection` pair): exploration(`updateExploration`)·stats(`tickStatsHistory`)·market(`updateMarket`)·scenarios(`checkScenarioGoals`)·victory(`checkWinConditions`+`checkProclamationGate`)·utilities(`tickUtilities`)·events(`tickRegionalEvents`)·elections(`checkElection`+`runElection`)·construction(`updateConstruction`)·scouts(`updateScouts`)·rival-ai(`updateRivalAI`)·expeditions(`updateExpeditions`). Wave 4 (fresh 10-agent scout) cleared the clean economy/faction ticks: statecraft(`collectVassalTribute`+`applyProvincePolicyEffects`)·naval(`navalTradeIncome`)·migration(`migrate`)·route-weather(`weatherRoutes`)·factions(`updateFactions`+`updateSettlementFactions`)·trade-season(`traders`+`caravans`). **Method: EXPOSE-ONLY** — flip each called private helper `private`→public and LEAVE it on RegionSim (called via `r.`), rather than co-extracting transitive clusters — keeps aiRng/rng draw order byte-identical, sidesteps the scout-flagged transitive risk on the 3/5 items. Exposed: buildingSight, stockOf, monthNeed, townEvent, autoPlaceCell, movePlayerScout/moveScout/invalidateFactionVisibility/spawnScout, rivalDiplomaticRound/factionTownOutput/maybeDevelopFactionTown, nextId/ordinal/blazeTrail/networkAnchor; exported REGION_BUILDINGS_MAP. Test-poke repoints: stats-history (silent `?.()` trap), phase17 (5 direct), region-wins (2 casts), phase14 (5 casts), region.test (1), wonders+wonder-race (2). Gates green per commit + full 1307 + vite build + bench (worst 13.99 ms). **★ NEW GOTCHA: `tsc --noEmit` does NOT typecheck `tests/`** — a missing test import (`tickUtilities is not defined`) passed tsc and failed only at runtime; verify test-poke repoints by RUNNING the affected test file, not tsc. **CLEAN TICK SUBSYSTEMS ESSENTIALLY EXHAUSTED.** Known-remaining tick methods NOT lifted: `fireEvent` (deferred — 16-line dispatcher needing 9 event-handler exposures; do as `systems/random-events.ts` co-extracting all 9); `monthlyEconomy`(~166ln)+`updateSectors`(~45ln) — the coupled economy CORE, own careful session; the opinion-media cluster (`tickMedia`/`tickOpinionDynamics`/`updateMediaReach`/`opinionVelocity`) — NOT a clean lift (UI-query entangled). The non-tick bulk (spatial/city-build, war/diplomacy ACTIONS, serialize/deserialize) needs interface-segregation, not the tick seam. **NEXT:** fireEvent→random-events.ts (mechanical); then economy core (own session); OR pivot to feature depth (Phase 10 climate sea-rise, Phase 14 zoning, Phase 16 warfare Front) / the on-map economy response to the now-live world market (consumer-demand increment 2). **Wave-4 gotchas:** methods whose loop binds a route to `r` (`weatherRoutes`/`traders`/`caravans`) need the param named `sim`; `formatCurrency` imports from `../defs` not `../region`; `ROUTE_CONDITION_FLOOR`/`REGION_BUILDINGS_MAP` export from region.ts.
>
> **PRIOR session (wave 2, PR #324): SIX tick subsystems lifted out (region.ts 14,969 → 13,985, −984).** Shipped (each its own commit, gated 1307 tests + tsc + vite build + serialize-determinism byte-identical, body verified line-for-line vs original): (1) `systems/monetary.ts` ← `tickMonetary`+`tickFX` (−192; exposed `computeCreditRating`+`depressionCeilingBonus`; exported `NEUTRAL_RATE`/`LEVERAGE_FRAGILITY`/`LEVERAGE_FRAGILE`/`FRAGILITY_GAIN`; repointed phase15/goods-prices/supply-cost-push). (2) `systems/military.ts` ← the 11 war ticks `updateArmyMovement`/`resolveProvinceBattle`(sibling)/`tickRivalArmyAI`/`tickMobilization`/`resolveArmyGroupBattle`/`tickSupplyLines`/`tickOccupation`/`tickWarSupport`/`consumeWarSupply`/`tickPlayerWar`/`abandonGhostTowns` (−478, the biggest single reduction; `tickPlayerWar` now called from `systems/diplomacy.ts`; exposed only `nextArmyId`+`_routePathCache`; the player ACTIONS + `computeCombatPower`/`computeWarScore` stay; repointed phase16). (3) `systems/historical.ts` ← `tickHistoricalAnchors` (−155; exposed `worldWarFired`/`oilShockFired`/`crashFired`/`crashMonthCounter`/`pandemicFired`; repointed historical-anchors/depression-cabinet/oil-shock-chain). (4) `systems/notables.ts` ← `tickNotableLifecycle` (−82; exposed `mintNotable`; repointed region.test). (5) `systems/{research,trade,charter,loans}.ts` ← `tickResearch`/`updateRouteCargo`/`updateCharter`/`updateLoans` (−77, batch commit; ZERO exposures, ZERO test pokes — the cleanest class). **Approach:** programmatic slice+transform (Python by signature regex; `this.`→`r.` + sibling-call fix; bottom-to-top delete with seam-blank collapse; `tsc` reveals private→public flips). **Pre-analyzed backlog for the NEXT session:** a 19-agent scouting workflow ranked ALL remaining tick methods cleanest-first with call-site/private-member/risk data — see `HANDOFF.md` → `## Recent session (2026-06-30) — Track-C wave 2`. Top picks: `updateExploration` (clean 5, expose `buildingSight`), `tickRegionalEvents`/`tickUtilities` (clean 5/4, expose `townEvent`), `tickStatsHistory`/`checkWinConditions`/`checkScenarioGoals` (clean 5, repoint pokes), `updateMarket` (clean 5, expose `stockOf`/`monthNeed`), `checkElection` (clean 4, pair with private `runElection`), `updateScouts` (clean 5 but 4 private helpers move too). SKIP: opinion-media (`tickMedia`/`tickOpinionDynamics`/`updateMediaReach`/`opinionVelocity` — UI-query entangled), `updateExpeditions`/`updateConstruction`/`updateRivalAI` (clean 3, multiple private non-tick helpers). **WEAK AREAS (this session):** (i) `npm ci` still fails on electron postinstall in this env → use `npm ci --ignore-scripts`. (ii) the cast-and-call test trap `(r as unknown as {m():void}).m()` / `(r as any).m?.()` SILENTLY no-ops after a method moves off RegionSim (tsc stays green, test passes while testing NOTHING) — always grep the method name in tests/ and repoint. (iii) region.ts is still ~14.0k lines / one class; the ~13 backlog ticks are the next ~600 safe lines, but the big residue (serialize/deserialize ~13k, the spatial city-build methods, the diplomacy/war ACTIONS) are query/action surfaces needing interface-segregation, NOT the tick-fn seam. (iv) the broader GAMEPLAY weak areas from session 6 still stand: world-market consumer-demand is live-but-telemetry-only (increment 2 = per-town price/consumption sink), inflation pinned 2.0% + satisfaction 0 across the sweep smell like dormant signals worth a probe, and the `RivalNation`↔`RegionalFaction` id-space unification is the multi-session rock.

---

**Session date:** 2026-06-30 (session 6) · **Repo:** aslater100/Centuria · **Tests:** 1307 passing (1292 + 15 `consumer-demand`) · serialize-determinism green · save-size green · tsc + vite build green · **this session: WORLD MARKET ACTIVATED — the consumer-demand structural fix (global-world leg 1, the headline unblock).** ★ **DIAGNOSIS (corrects ~15 sessions of framing): the world market was STRUCTURALLY DEAD, not "balanced-dormant".** `localGoodDemand`/`worldGoodDemand` count only intermediate-INPUT demand (normalized sector-shares, O(1)/good) with NO final-consumption sink, while production deposits `baseOutput × level`/tick → the 8 TERMINAL goods (food/clothing/tools/vehicles/machinery/consumer_goods/pharmaceuticals/luxury_goods) have ~0 demand, every good oversupplies ~baseOutput×, stocks accumulate UNBOUNDED (probe seed 1000 @2100: steel supply 2881 vs demand 1.83 = 1574×), and `worldGoodScarcity = 1 − supply/demand` was pinned at 0 FOREVER regardless of specialisation/war (so "bites under specialisation" was FALSE for the world market). **FIX:** flag-gated CONSUMER-DEMAND model (`r.consumerDemand`, default OFF, NOT serialized — the `autoDevelopPlayer` pattern). On → the world market reads a FLOW signal: this-tick production capacity (`baseOutput × level`, via a new transient `r.goodLevels` cache set in `tickIntermediateGoods` from `result.levels`) vs an EXOGENOUS final demand (`finalGoodDemand = baseOutput × FINAL_APPETITE` 1.0, level-independent) tilted by the great powers → a shock (level<1)/war/warming finally lifts world scarcity. `FINAL_APPETITE` 1.0 = balanced boundary (0 in healthy play, +ve under any stress; commercial surplus floors at 0). **Implementation:** `worldGoodDemand`/`worldGoodScarcity` branch on `r.consumerDemand` (legacy stock path VERBATIM when off → byte-identical); `worldMarketTightness` routed through `worldGoodScarcity`; new `goodBaseOutput` memo; all in `systems/goods.ts`. **BYTE-IDENTICAL off:** 1307 tests, determinism + save-size green, vite build green. **ACTIVATION (8-seed×181y `SIM_CONSUMER_DEMAND=1`):** `wMkt%` 0.0 → LIVE 3.6–6.7% tracking `gpP%` (1042: gpP 7.8%→wMkt 6.7%); on-map serialized economy BYTE-FOR-BYTE identical to OFF (uniform anchor lift → no price gaps → no flow change → telemetry-only). New `tests/consumer-demand.test.ts` (15: finalGoodDemand off/on/locked/raw; THE STRUCTURAL FIX — a shock registers despite a 10k-unit hoard where the stock model pins at 0; balanced boundary; monotone+bounded; war/climate/commerce tilt channels; byte-identical-off; determinism; live integration incl. on-vs-off `serialize()` equality). Adversarial verify-workflow run on the diff (no blockers). **NEXT (increment 2 — the bigger re-baseline that makes "one global economy" FELT, wants a human playtest):** flow-based PER-TOWN `localGoodPrice` (its LOCAL part is still stock-based → swamped by unbounded inventory, so on-map towns don't respond to the now-live world signal) + a per-town consumption SINK draining stock → specialised towns run short → world scarcity → local price → arbitrage → REAL on-map flows; then feed world tightness/`localGoodsScarcity` into the MACRO coupling (cost-push/output-drag) and flip `consumerDemand` ON by default. **WEAK AREAS for next session:** (i) on-map economy inert to the live world signal (increment 2 above); (ii) **inflation pinned at EXACTLY 2.0% & satisfaction 0 in EVERY seed across the whole 8×181y sweep** — both smell like a clamped/dormant signal (the macro `localGoodsScarcity` cost-push is still 0; sat=0 may be a late-game climate artifact OR a real bug) — worth a focused probe; (iii) leg-2 `RivalNation`↔`RegionalFaction` id-space unification (multi-session — DON'T bulldoze; a read-only derived bridge is the safe first slice); (iv) 44-good catalog (own session). **PRIOR session 5 below.**

**Session date:** 2026-06-30 (session 5) · **Repo:** aslater100/Centuria · **Tests:** 1274 passing · serialize-determinism green · save-size green (ceiling re-baselined 192→224 KiB; actual ~198 KiB) · PR #320 open · **this session: CLIMATE REFUGEE MIGRATION + ANNUAL STATS HISTORY + AUTONOMOUS RIVAL CLIMATE ACCORD OFFERS.** Three features, all byte-identical in balanced autoplay. **(1) Climate refugee migration** (`tickClimate()` refugee block): coastal unwalled player settlements bleed population to the largest inland settlement when `year ≥ 2035 && warmingC > 1.5`. Flight rate: `min(1%, 0.1% × severity)` per monthly tick; drowned-era 1.5× multiplier. `removePop()` debits cohort bands; inland gains bands[1]+bands[2]. `lastRefugeesLogDay` field serialized/backfilled (`?? -999`). Gate: year ≥ 2035 AND warmingC > 1.5 AND coastal AND !seaWall AND popOf(t) > 5 AND inland exists. Byte-identical: headless runs end around 2035–2100 with seeds not producing coastal+unwalled+high-warming combos in the serialize-determinism fixture (which uses no `pref:'coastal'` and short warm-up). 7 tests in `tests/climate-refugee-migration.test.ts`. **(2) Annual stats history — Century Graph data layer (GDD §8.7)**: new `StatSnapshot` interface (`{year,gdp,pop,warmingC,treasury,satisfaction}`) + `STATS_HISTORY_MAX = 200` ring buffer (exported constant) + `statsHistory: StatSnapshot[]` field on RegionSim. New private `tickStatsHistory()` called from `monthlyUpdate` when `month === 0` (January). Serialized as `statsHistory` / backfilled `?? []` for old saves. Ring capped with `shift()`. Save-size grew ~5.6 KiB (181 snapshots × ~31 bytes) → re-baselined `CENTURY_CEIL_KIB` from 192 → 224 in `tests/save-size.test.ts` (actual ~198 KiB, generous headroom). 11 tests in `tests/stats-history.test.ts`. **(3) Autonomous rival climate accord offers (GDD §8.2 / §6.3)**: new `ARCHETYPE_GREEN_PROPENSITY` map (trading_republic=1.0, crusader_state=0.9, opportunist=0.5, hegemon=0.2, hermit_kingdom=0.1) exported from region.ts. In `tickDiplomacy()` AI-offers block, a new `else if` branch fires when: `!rv.treaties.includes('climate_accord') && accordUnlocked() && warmingC > 1.8 && year >= 2020 && propensity >= 0.6 && rng.chance(0.06)`. Offer: `{rivalId, kind:'climate_accord', expiresDay: day+180}` (180-day window vs 90-day trade offers). Byte-identical: `accordUnlocked()` requires `has('environmentalism')` which the headless fixture (`central_bank_charter + income_tax`) never unlocks → branch never fires → RNG stream unchanged. 9 tests in `tests/rival-accord-offer.test.ts`. **WEAK AREAS for next session:** (i) C1 extraction of `tickClimate()` or `tickAccords()` — complex due to private deps (`removePop`, `addLog`, `warmingC`, `settlements`, settlement fields) — would need a shim or full class move; (ii) human playtest for late-game climate (refugee waves, brownout at 3°C, overtopping at 4°C — all unverified by human); (iii) `RivalNation`↔`RegionalFaction` id-space unification (multi-session redesign — DON'T bulldoze); (iv) 44-good catalog (own session). **NEXT:** C1 extraction of a warfare/climate subsystem; OR human late-game playtest; OR id-space bridging (small first step: give `RegionalFaction` an `archetype` field mirroring the nearest rival). **Previous session:** · (session 4) sea-wall overtopping + war-scar relations + brownout UI + tuning pass · **1247 tests** · PR #320 open. More below.

**Session date:** 2026-06-30 (latest) · **Repo:** aslater100/Centuria · **Tests:** 1210 passing · tsc clean · vite build green · serialize-determinism green · save-size green · headless 8-seed×181y **economy BYTE-IDENTICAL to HEAD** (towns/treasury/GDP/treas-GDP/infl/pop/sat/pBld/wMkt% all match; new `gpP%` column now shows live great-power pressure −1.6%…+7.8% across seeds) · **this session: THE GREAT POWERS JOIN THE WORLD MARKET (global-world leg 1; byte-identical in balanced play, bites under shortage).** User: *"continue the work, deeply. agentic. accurate. Compress often. Avoid token bloat. Identify weak areas. work on the biggest lift stuff, most impact."* The headline arc is "make the world GLOBAL not REGIONAL"; leg 1's world market (`worldGoodSupply/Demand/Scarcity/Price/Tightness`, `systems/goods.ts`) only ever iterated the ON-MAP towns (`r.settlements`), so the off-map `RivalNation` great powers — nation-scale economies that fight, trade, and burn carbon — sat OUTSIDE the one world market ("rivals trade in no market at all"). This slice makes them ECONOMIC PARTICIPANTS. **Implementation (`systems/goods.ts`):** new `rivalNetDemandTilt(r,goodId) ∈ [−0.6,+0.6]` = Σ over `r.rivals` of `(pop/RIVAL_WORLD_POP_NORM 50000) × netImport`, clamped to `RIVAL_WORLD_TILT_CAP` 0.6. `netImport` has THREE derived channels: (1) **WAR** — a rival in the `foreignWars` ledger imports `RIVAL_WAR_IMPORT` 0.5 (war strips its output, its appetite swells → it pulls on the world's goods, dear for everyone — the WWII-rationing channel); (2) **COMMERCE** — `−RIVAL_COMMERCE_TILT 0.4 × (weights.commerce − RIVAL_COMMERCE_MEAN 4.5)/9`, so a Trading Republic (commerce 9) EXPORTS/relieves and a Hermit Kingdom (commerce 2) IMPORTS/hoards; (3) **CLIMATE** — agri-attributed goods (`goodProducingSector==='agriculture'`: textiles; food is terminal) gain `RIVAL_CLIMATE_FOOD_IMPORT 0.4 × min(1, warmingC/RIVAL_CLIMATE_C 3.0)` (climate hits the GLOBAL breadbasket, leg-3's shared shock reaching the market). `worldGoodDemand` now returns `onmap × (1 + tilt)` (early-returns the bare sum for an un-demanded good, so rivals can't conjure appetite from nothing). New telemetry `worldPowerPressure(r) ∈ [−0.6,+0.6]` (demand-weighted mean tilt) + public `r.worldPowerPressure()` wrapper (region.ts ~L7299) + `headless.ts` `gpP%` column. **DERIVED, PURE, no new serialized field, no RNG:** reads only already-serialized `pop`/`weights.commerce`/`foreignWars`/`warmingC` → `worldGoodDemand` stays pure; save-size + serialize-determinism untouched. **BYTE-IDENTICAL economy in balanced play (proven, the gold standard):** the ONLY sim consumer of the world market is `worldGoodScarcity → localGoodPrice` one-sided anchor → `tickPriceArbitrage` (it ships ONLY on a price GAP, `arbitrage.ts:143/150`). In self-sufficient autoplay every on-map town has `localScar=0`, so any world-scarcity change lifts ALL town prices UNIFORMLY → no gaps → no flows → the serialized economy is byte-for-byte HEAD (verified: pre-column-add `npm run sim -- 181 8` diff = IDENTICAL incl. wMkt% 0.0). Plus on-map stock dwarfs demand → `worldGoodScarcity` stays 0 regardless of the tilt, so wMkt% holds 0.0 too. It BITES only when the on-map world is itself non-uniformly short (specialisation/downturn): a great-power war then raises `worldGoodScarcity` → the world anchor reroutes arbitrage / lifts even a FLUSH town's price (proven in tests). New `tests/rival-world-market.test.ts` (18: inert-without-powers; war ×1.5 @pop-norm; peace-mean-commerce==base; commerce export ×0.8 / hermit import ×1.111; climate textiles ×1.4 @3°C & ×1.2 @1.5°C, steel untouched; cap ±0.6 both ways; pop-weight linearity; the BITE — war raises scarcity+price & lifts a flush town's localGoodPrice; byte-identical-when-flush; pressure sign war+/trade−; bounded; determinism; live-autoplay finite+slack). **Gates:** tsc clean, vite build green, **1210 tests** (1192+18), serialize-determinism green, save-size green, headless 8-seed×181y economy byte-identical to HEAD. **GOTCHA:** `food` is a TERMINAL good (no intermediate consumes it as input → `localGoodDemand('food')==0` always → the climate channel can't bite it); `textiles` is the agri good with real chain demand — test/reason with textiles, not food. **WEAK AREAS for next session:** (i) the great-power participation is DORMANT in today's balanced sweep (like ALL goods coupling — `localGoodsScarcity==0`, wMkt 0.0%); it only BITES once the on-map world genuinely specialises into shortage — so its real-economy effect is still UNMEASURED in a live long-run (proven only in constructed unit tests + the visible gpP% telemetry). The activating re-baseline is the long-deferred **human downturn playtest** / a forced-specialisation live probe. (ii) leg 2 = `RivalNation`↔`RegionalFaction` id-space unification (still the multi-session redesign — DON'T bulldoze). (iii) player auto-EXPAND in autoplay (flag-gated, single-town today). (iv) 44-good catalog (own session). **NEXT (global-world leg 1 follow-on / re-baseline order):** (a) a FORCED-SPECIALISATION live probe or downturn playtest that drives `localGoodsScarcity>0` so the great-power tilt's arbitrage reroute is MEASURED end-to-end (converts "dormant" → "measured under stress"); (b) consider letting a great-power war/commerce posture also feed the MACRO `localGoodsScarcity` coupling (cost-push/output-drag) — a deliberate re-baseline; (c) leg 2 id-space bridge. PRIOR session below.

**Session date:** 2026-06-30 (session 4) · **Repo:** aslater100/Centuria · **Tests:** 1247 passing · serialize-determinism green · PR #320 open · **this session (continued): sea-wall overtopping + war-scar relations + brownout UI + full tuning pass.** Sea-wall overtopping: at warmingC ≥ 4.0°C and year ≥ 2060, even walled coastal towns breach (0.3x–0.6x damage scale vs unprotected; flood-proofed 0.3x). tests/seawall-overtopping.test.ts (5). War-scar relations: after player victory, defeated rival loses 30+5×occupied relations + noteHistory; after defeat, victorious rival gains +15 relations. Revanchism advisor event: Foreign Secretary surfaces at 4%/mo when defeat warScar + revanchism CB available. tensionScalar: warmingC>2°C → up to 0.45 tension; drowned/dystopia → 0.25 floor. Brownout UI: `industryClimateMult()` displayed in climate panel when <1 (matches agriClimateMult pattern). WAR_SUPPORT_DECAY_MULT tuned: direct_democracy 1.5x → fascist 0.55x (accountability model). ARCHETYPE_WAR_FREQ_MULT tuned: hegemon 1.6x, hermit 0.3x, trading_republic 0.4x. tests/archetype-war-freq.test.ts (8). **1247 tests.** **NEXT:** C1 extraction of tickAccords or tickClimate (complex due to private deps — may need shim); climate-refugee migration into inland settlements (when coastal pop flees); headless re-baseline run to measure ARCHETYPE_WAR_FREQ_MULT impact on era outcomes. **Previous session:** · (1) WAR_SUPPORT_DECAY_MULT tuned with regime accountability model: direct_democracy 1.5x → fascist 0.55x; full ordering by political accountability. (2) ARCHETYPE_WAR_FREQ_MULT tuned: hegemon 1.6x, trading_republic 0.4x, hermit_kingdom 0.3x, crusader_state 1.2x, opportunist 1.1x. (3) tensionScalar() extended: climate dread above 2°C adds up to 0.45 ambient tension; dystopia/drowned era branches add 0.25 floor. (4) Revanchism advisor event: when a defeat warScar exists and revanchism CB is available, Foreign Secretary surfaces it monthly at 4% probability. New tests/archetype-war-freq.test.ts (8). Updated tests/d2-mil-scaffold.test.ts (22, was 19: replaced all-1.0 assertions with accountability-ordering tests). **1242 tests.** **NEXT:** C1 extraction of tickClimate or a warfare system; sea-wall overtopping with pop-flight mechanics; tune the non-1.0 values after headless re-baseline. **Previous session:** · (1) WAR_SUPPORT_DECAY_MULT (all-1.0, no-op scaffold, mirrors WAR_SUPPORT_FLOOR key set). (2) `front?:{position:number}` on PlayerWar; `tickPlayerWar` sets `w.front={position:w.score}` after each score update (write-only). (3) warScars bookkeeping — `WarScar` interface + `warScars:WarScar[]=[]` on RegionSim; `recordWarScar()` helper called at all 4 war-end paths; serialize/deserialize with `??[]` backfill. (4) Revanchism CB — added to BOTH `CasusBelli` and `CBType` unions; `CASUS_BELLI_DEFS.revanchism` (support 85); `availableCasusBelli` gates on defeat warScar against same rival; `generateCasusBelli` exposes it. (5) ARCHETYPE_WAR_FREQ_MULT (all-1.0 scaffold) applied in AI war-declaration. (6) Era skin: `root.dataset.era = region.eraBranch??''` in main loop; CSS `[data-era]` selectors for solarpunk/dystopia/drowned. (7) **Industry brownout climate drag** (Tier-2 re-baseline): `INDUSTRY_BROWNOUT_THRESHOLD=3.0°C`, `INDUSTRY_BROWNOUT_SLOPE=0.10`, `INDUSTRY_BROWNOUT_MAX_DRAG=0.30`; `industryClimateMult()` mirroring `agriClimateMult()`; wired into `updateSectors` via climateMult for `id==='industry'`; byte-identical below 3°C. Fixed `tests/climate-agriculture.test.ts` (test assumed industry was NEVER affected; split into two tests: below-brownout-threshold → industry untouched, above-threshold → industry dragged). **GOTCHA — dual CB type unions:** there are two separate CB type unions in region.ts — `CasusBelli` (~line 1821, used by `availableCasusBelli`/`declareWar`) and `CBType` (~line 1932, used by `generateCasusBelli`/`CBDef`). Adding a new CB MUST add to BOTH unions or TypeScript errors. **Tests: 1231 passing.** Serialize-determinism: green (brownout is a new Tier-2 re-baseline but warmingC stays <3°C in typical autoplay). **NEXT:** tune WAR_SUPPORT_DECAY_MULT/ARCHETYPE_WAR_FREQ_MULT non-1.0 values after human playtest; sea-wall overtopping/climate-refugee migration (Tier-2); C1 extraction warfare → systems/warfare.ts; RivalNation↔RegionalFaction id-space unification (multi-session). **Previous session:** · tsc clean · vite build green · serialize-determinism green · save-size green · headless 8-seed×181y **BYTE-IDENTICAL to HEAD** · **this session: CROSS-FACTION TRADE + BALANCE-BOUND GUARD (global-world leg 1 step a; byte-identical in balanced autoplay).** User: *"continue the work, deeply. agentic. accurate. Compress often. Avoid token bloat. Identify weak areas."* Two items from the prior baton shipped: **(a) cross-faction trade** (leg 1 step a) and **(d) balance-bound guard** (locking the spatial-AI re-baseline). **Cross-faction trade (`systems/arbitrage.ts` + `src/sim/region.ts`):** previously `tickPriceArbitrage` filtered to only the player's settlements (`s.factionId === r.playerFactionId`), so rival towns traded in no market and inter-rival surplus/shortage stayed unresolved; delivery income always went to `r.treasury` regardless of source faction. Fix: (1) dropped the `playerFactionId` filter — **all settlements (every faction) now participate in arbitrage** dispatch; rival towns trade intra-faction AND cross-faction if a route connects them. (2) Delivery **credits the SOURCE faction's treasury** via new public `RegionSim.addFactionTreasury(factionId, amount)` (`region.ts`): routes to `this.treasury` (player national) or `faction.treasury` (rival), mirroring the private `factionDevPurse`/`spendFactionDev` pair — it's the public complement for use outside the class. (3) RNG gate unchanged on `totalDelivered > 0` (any faction delivery); log fires only when `playerDelivered > 0` — minimal RNG-stream perturbation. **BYTE-IDENTICAL in balanced autoplay:** self-sufficient play → no price gaps → no flows → byte-for-byte to HEAD (proven: 8-seed×181y headless; determinism + save-size green). New `tests/cross-faction-trade.test.ts` (13: `addFactionTreasury` routing player/rival/unknown/no-op; rival-town dispatch when route+gap exist; no dispatch without route or self-sufficient; rival treasury credited on rival delivery; player treasury credited on player delivery; player treasury untouched when only rival flows deliver; cross-faction player↔rival route dispatches flow; inert-in-balanced-play; determinism). **Balance-bound guard:** 3 new tests added to `tests/player-autodevelop.test.ts` (seeds 1000/1007/1021, 100y autoplay with `autoDevelopPlayer=true`): treasury 1–30 months of GDP (probe: 6.7–10.3), pop 5k–50k (probe: 15.6k–17.1k), inflation 1–4% (~2%), player builds ≥3 placed buildings/districts — locks the spatial-AI re-baseline against silent drift. Gates: tsc clean, vite build green, **1192 tests** (1176+16), serialize-determinism green, save-size green, headless 8-seed×181y **BYTE-IDENTICAL to HEAD**. **GOTCHA for next session:** rival treasury (`faction.treasury`) is the faction's OWN purse — distinct from the player national `r.treasury` — so `addFactionTreasury` correctly routes to each. The `factionDevPurse`/`spendFactionDev` pair (private, spatial AI spend) and `addFactionTreasury` (public, arbitrage credits) are symmetric seams around this split. **WEAK AREAS for next session:** (i) leg 2 = `RivalNation`↔`RegionalFaction` id-space unification (multi-session redesign — DON'T bulldoze in one session); (ii) human downturn playtest (goods coupling, agri-climate drag, spatial balance changes — all unverified by human); (iii) player auto-EXPAND in autoplay (flag-gated; bigger spatial re-baseline than auto-develop; player still single-town); (iv) 44-good catalog (own session). **NEXT:** (b) leg 2 id-space unification, OR pivot to the human playtest + spatial tune, OR 44-good catalog. PRIOR session below.

**Session date:** 2026-06-30 (prev) · **Repo:** aslater100/Centuria · **Tests:** 1176 passing · tsc clean · vite build green · serialize-determinism green · save-size green · headless 8-seed×181y **BYTE-IDENTICAL to HEAD** (wMkt 0.0%) · **this session: WORLD-PRICE ANCHOR — `localGoodPrice` now reflects WORLD scarcity (global-world leg 1, SLICE 2; byte-identical in balanced play).** User: *"continue the work, deeply. agentic. accurate. Compress often. Avoid token bloat. Identify weak areas."* Picked the prior baton's leg-1 NEXT-(a): "anchor `localGoodPrice` to `worldGoodPrice`". **Implementation (`systems/goods.ts`):** new exported **`worldGoodScarcity(r,goodId)` ∈[0,1]** = `stockScarcity(worldGoodSupply, worldGoodDemand)` (the town-independent world-shortage driver; `worldGoodPrice` refactored onto it BIT-IDENTICALLY) + public `r.worldGoodScarcity` wrapper (region.ts ~L7283). `localGoodPrice` gains a ONE-SIDED world anchor: `eff = localScar + WORLD_PRICE_ANCHOR·max(0, worldScar − localScar)`, `WORLD_PRICE_ANCHOR` 0.5 (new const, goods.ts ~L78). **Why one-sided (`max(0, world−local)`):** it only ever RAISES a town's effective scarcity toward the world's, so (i) it NEVER lowers a locally-short town below its local-only price → the local relief arbitrage ships on is preserved verbatim, and (ii) a locally-FLUSH town gains the global-scarcity premium when the WORLD is short (a globally-scarce good is dear even on a full shelf). `eff ∈ [0,1]` → price stays bounded `[base, 2×base]`. **BYTE-IDENTICAL in balanced play:** `localGoodPrice` is consumed ONLY by arbitrage (`arbitrage.ts:128`); in autoplay every town is self-sufficient → worldScar 0 for every good → lift +0 → arbitrage unchanged (proven: headless 8-seed×181y byte-for-byte to HEAD; determinism + save-size green). It bites ONLY when the WORLD is collectively short — never in self-sufficient autoplay — so it's a **zero-re-baseline substrate** that changes WHICH lanes ship only under genuine global specialisation. **Re-baseline (TEST-ONLY):** one `goods-prices.test.ts` unit test (`un-demanded good priced at base regardless of stock`) updated — under the anchor an un-demanded good is no longer flat-base when the WORLD is short of it (the anchor biting by design); isolated by stocking the other town so the world stays self-sufficient for that good. New `tests/world-anchor.test.ts` (8: dormant-when-slack, live-autoplay-0, lift-toward-but-not-past-world, ceiling-invariant @full-local-scarcity, never-below-local @partial, bounded, determinism, wrapper-parity). **★ FINDING (corrects prior baton's leg-1 (c)):** the specialisation/coupling stress-probe is ALREADY substantially covered by `goods-prices.test.ts` §3–5 (localGoodsScarcity>0 & bounded for a specialised multi-town nation; cost-push activation + the 0.50 ceiling; the EXACT `(1−scarcity×DRAG)` output-drag) — only a live-`tick()` long-horizon version is missing (organic specialisation fights the auto-develop AI; deferred). **GOTCHA:** arbitrage runs MONTHLY (region.ts:5833, inside the monthly block — NOT per-tick), so the world-scarcity loop inside `localGoodPrice` is cheap; `worldGoodScarcity` is O(settlements²×goods) via `worldGoodDemand`→`localGoodDemand`→`sectorShare`, but the monthly cadence makes it a non-issue (bench unaffected). **NEXT (global-world leg 1):** (a) **CROSS-FACTION trade** — drop the `playerFactionId` filter in `tickPriceArbitrage` so arbitrage clears at `worldGoodPrice` between ANY factions' towns, crediting the correct purse via the existing `factionDevPurse`/`spendFactionDev` seam (rivals become market participants — the headline remaining RE-BASELINE); (d) **flag-ON `autoDevelopPlayer` balance-bound guard test** (weak area iii) to LOCK the spatial-AI re-baseline; leg 2 = the `RivalNation`↔`RegionalFaction` id-space unification (multi-session redesign — DON'T bulldoze). PRIOR session below.

**Session date:** 2026-06-30 (prev) · **Repo:** aslater100/Centuria · **Tests:** 1168 passing · tsc clean · vite build green · serialize-determinism green · save-size green · headless finite/stable (wMkt 0.0%) · **this session: WORLD MARKET reference price — LEG 1 of the global-world arc (byte-identical substrate).** User: *"continue the work, deeply. agentic. accurate. Compress often. Avoid token bloat. Identify weak areas."* The headline weak area from the last several batons is that the world is **REGIONAL not GLOBAL**. A probe (Explore agent) confirmed the exact shape: (1) **arbitrage/trade is player-nation-only** — `tickPriceArbitrage` filters to `r.settlements.filter(s => s.factionId === r.playerFactionId)` (`arbitrage.ts:108`), so rivals trade in NO market and there is no single world clearing price; (2) goods PRODUCTION already spans all factions (`distributeGoodProduction` / `localGoodsScarcity` iterate `r.settlements`), but consumption/price reach only the player's macro. **★ The probe also corrected this handoff: leg 3 — "climate as a shared shock" — is ALREADY DONE.** Warming bites EVERY faction's output uniformly — `agriClimateMult` (≥1.5 °C, ≤30%) is applied in `updateSectors(t)` which loops ALL settlements (region.ts ~L7250), the subsistence-food `climateDrag` (≥0.8 °C, ≤35%) hits every settlement (~L5489), and emergency grain/famine pays from each town's OWN faction purse — no per-faction exemption. So the arc narrows to leg 1 (world market) + leg 2 (id-space unification). **Fix — first global-market layer (`systems/goods.ts`):** `worldGoodSupply(r,goodId)` (Σ stock across EVERY faction's towns) + `worldGoodDemand(r,goodId)` (Σ `localGoodDemand` across all settlements) feed **`worldGoodPrice(r,goodId)`** = `goodBasePrice × (1 + stockScarcity(worldSupply, worldDemand) × LOCAL_GOODS_PRICE_GAIN)` — the EXACT `localGoodPrice` curve lifted from one town to the whole world (base when the world is self-sufficient, up to base×2 when collectively starved, raw/un-tracked → base). **`worldMarketTightness(r)` ∈ [0,1]** = demand-weighted mean world scarcity over unlocked goods. Public read-only wrappers `r.worldGoodPrice`/`r.worldMarketTightness` (region.ts ~L7275, right after `sectorOutputOf`); `headless.ts` adds a `wMkt%` column. **Pure/read-only → BYTE-IDENTICAL:** no serialized field, no tick math, consumed by telemetry only; in balanced self-sufficient autoplay every town holds its own demand → world supply ≥ demand → tightness 0 → price == base (mirrors the existing per-town dormancy; wMkt reads 0.0% across the sweep). New `tests/world-market.test.ts` (13): world supply/demand span ALL factions incl. a `faction:99` rival town; a rival surplus relieves the world price for everyone (cross-faction clearing); clearing curve base↔base×2 at GAIN 1.0; raw→base; tightness 0-when-self-sufficient / >0-&-bounded-when-short / eases-as-supplied / deterministic; live-autoplay finite & 0. **Gates:** tsc clean, vite build green, **1168 tests** (1155+13), serialize-determinism green, save-size green, headless finite/stable. **GOTCHA — leg 2 is a redesign, not a slice:** `RivalNation` (off-map great powers: `archetype`, `relations`, `treaties`, `compass`, `pop`) and `RegionalFaction` (on-map territorial AI: `settlementIds`, `treasury`, `regime`, `techFocus`) are GENUINELY DIFFERENT ENTITIES in separate id-spaces with no shared key — not two ids for one thing — so "unifying" them means giving on-map factions diplomacy/archetype OR giving rivals territory, a multi-session restructure with real determinism risk. Don't bulldoze it in one session. **NEXT (global-world leg 1, byte-identical → re-baseline order):** (a) **anchor `localGoodPrice` to `worldGoodPrice`** (a blend) so a town's price reflects WORLD scarcity — byte-identical in balanced play, bites under specialisation; (b) **CROSS-FACTION trade** — let arbitrage clear at `worldGoodPrice` between ANY factions' towns, crediting the correct purse (drop the `playerFactionId` filter) so rivals become market participants (re-baseline); (c) a **specialisation/coupling stress-probe TEST** that forces a multi-town specialised nation and asserts the long-dormant goods coupling (scarcity>0, cost-push, output-drag) ACTIVATES & stays BOUNDED — converts "dormant-unmeasured" into "measured-under-stress" with NO live re-baseline (the recurring weak area, finally measurable); (d) **flag-ON `autoDevelopPlayer` balance-bound guard test** to LOCK the spatial-AI re-baseline. **— Prior session: PLAYER FACTION AUTO-DEVELOPS IN AUTOPLAY (closes spatial-AI NEXT-b; flag-gated, default OFF → live human play byte-identical; headless re-baseline ON).** User: *"continue the work, deeply. agentic. accurate. Compress often. Identify weak areas."* The flagged NEXT-b: rivals played spatially but the PLAYER faction was skipped by `updateRivalAI` (a human builds it manually), so in autoplay the player held **ONE BARE town — 0 placed buildings, 0 districts, 0 legacy buildings** (probe-confirmed) and its whole 19k-pop/1.2M-treasury economy ran on raw terrain yields → the headless balance signal measured the spatial economy ONLY via rival competition, never the player's own path. **Fix — `autoDevelopPlayer` flag (`src/sim/region.ts` ~L3133, public, NOT serialized):** when set, the player faction reuses the SAME develop logic as a rival. (1) **Purse seam** — `factionDevPurse(faction)`/`spendFactionDev(faction,cost)` (~L14390) return/debit the NATIONAL `this.treasury` for the player (what a human spends building via the UI), the faction's own `treasury` for a rival → for a rival they read/write exactly `faction.treasury` in the same order, so **the rival path stays byte-identical** (proven: serialize-determinism green + flag-OFF headless diff matches HEAD verbatim). (2) Renamed `maybeDevelopRivalTown`→**`maybeDevelopFactionTown`** + routed `tryBuildRivalBuilding`/`tryZoneRivalDistrict` through the purse. (3) In `updateRivalAI` (~L14888) the player branch now, when `autoDevelopPlayer`, calls `maybeDevelopFactionTown(pf, aiKnobs(), factionTownOutput(pf))` on the player's staggered cadence — NOT the full rival AI (no goals/expansion/military/diplomacy: those stay the human's). Reserve-gated against the player's own town output exactly like a rival (the player's emergency grain is paid from `this.treasury` too, so the 0.5-mo famine floor protects the same buffer). **headless.ts turns the flag ON by default** (env `SIM_PLAYER_MANUAL=1` restores the passive baseline) + adds a `pBld` column. **Re-baseline (8-seed×181y, flag ON):** player now builds **10–15 placed buildings/districts** (was 0) — buildout SATURATES fast (one town, finite worked ring + per-building maxes); pop **21.6–22.9k** (was 19.0–21.4k — spatial output supports more people), treasury **8.9–12.6 mo of GDP** (bounded), infl pinned **2.0%**, outcomes **4 drowned / 4 dystopia** (same split), towns 13–37. Mid-game economy notably STRONGER (e.g. seed 1028 treasury 62k vs 32k @2000, 201k vs 84k @2040). Satisfaction is volatile 0↔80 across the century in BOTH modes (late-game climate noise, not a regression). New `tests/player-autodevelop.test.ts` (9: purse routing player-vs-rival, famine-floor reserve gate through the national purse, flag-OFF→player stays bare, flag-ON→player builds funded from national treasury w/ faction purse untouched, determinism). **NEXT:** spatial-AI NEXT-a + NEXT-b BOTH DONE. Remaining spatial: **(c)** tune `BUILD_LEAN_*`/`EXPAND_LEAN_SCALE`/`RIVAL_DEV_RESERVE_MONTHS` after a human playtest. **⚠️ WEAK AREAS for next session:** (i) **THE HEADLINE ARC IS NOW THE TOP STRUCTURAL ITEM — world is REGIONAL not GLOBAL** (full note below: one world market, unified `RivalNation`↔`RegionalFaction` id-spaces, climate as a shared shock). With both spatial-AI nexts closed, sequence the global-world arc next. (ii) The autoplay player still holds **ONE town** — it auto-DEVELOPs but does NOT auto-EXPAND (deliberately; expansion is a much bigger re-baseline that fills the map differently), so the player's spatial exercise is single-town. Letting the player found a 2nd/3rd town in autoplay (still flag-gated) would exercise multi-town districts + the inter-town goods/arbitrage path on the PLAYER's side. (iii) Only the `serialize-determinism`+`player-autodevelop` determinism tests PIN the flag-ON behavior; no automated test asserts flag-ON *balance bounds* (treas/GDP, pop) — the automated `economy-balance.test.ts` still runs the manual-player game. A flag-ON long-run bound guard would lock the re-baseline. **— Prior session (NEXT-a): PERSONALITY STEERS RIVAL DISTRICT CHOICE + EXPANSION SITING (intentional re-baseline).** User: *"continue the work, deeply. agentic. accurate. Compress often."* The immediate flagged next (NEXT-a): last session made the rival BUILDING pick personality-aware via `factionBuildLean`, but the two other spatial decisions stayed personality-blind. **Fix — extend the SAME lean to both** (`src/sim/region.ts`): (1) **`tryZoneRivalDistrict`** (~L14474) now scores districts `cluster + lean[d.sector]` behind a hard `cluster ≥ 2` gate — equal-size clusters in different sectors are broken by personality (a liberal zones `commercial_district` where an autocrat zones `industrial_park`), but the lean (< 1) can NEVER outweigh a strictly larger cluster, so it only tips ties. (2) **`findBestExpansionSite`** (~L15176) adds a `leanPull` that maps the sector lean onto the terrain that feeds it — agri→`isPlains||isRiver`, industry→`isMountain||isForest`, services→`isCoastal||isRiver` — times new `EXPAND_LEAN_SCALE` 30 (region.ts ~L701), so a Merchant Republic gravitates to the coast, a Junta to the hills, a Monarchy to the plains, while the goal bias (+20) and spacing penalty (−100) still dominate. **KEY: both read ONLY existing serialized faction fields — NO RNG draw, NO new field** → the `aiRng` draw ORDER is byte-identical (only the SELECTED district/tile changes downstream); determinism + save-size gates stay green. **Re-baseline measured vs HEAD (8-seed×181y):** outcomes UNCHANGED (4 drowned / 4 dystopia, no seed flipped); 4 seeds byte-identical (1000/1014/1021/1028), 4 shifted modestly via competition (1007/1035/1042/1049: towns ±3-4, treasury/pop within a few %) — all bounded/finite, no collapse/balloon. New tests `tests/rival-development.test.ts` (27→33, +6): district lean tie-break (trad→farming vs liberal→commercial on equal clusters; larger cluster still wins regardless of lean), expansion determinism, **non-vacuous agrarian-vs-industrial siting divergence across 40 seeds** (both factions evaluate the SAME sampled tiles + noise on a fresh same-seed colony, so the lean is the only differentiator). **NEXT (spatial AI):** NEXT-a DONE. Remaining: **(b)** PLAYER-faction auto-develop in autoplay behind an explicit flag (the headless balance signal is still rival-centric — a passive player under-exercises its OWN spatial path); **(c)** tune `BUILD_LEAN_*`/`EXPAND_LEAN_SCALE` after a human playtest. **⚠️ WEAK AREAS noted this session for the future:** (i) **the headline arc is still open — world is REGIONAL not GLOBAL** (full note below — one world market, unified `RivalNation`↔`RegionalFaction` id-spaces, climate as a shared shock); sequence it after NEXT-b. (ii) Expansion personality is SPARSE by design — with only 5–8 Monte-Carlo samples, a faction's preferred terrain often isn't among the sampled tiles, so siting divergence is rarer than building divergence; stronger spatial personality would mean biasing the SAMPLE distribution (not just the score), a bigger re-baseline. (iii) The expansion divergence test is statistical (≥1 of 40 seeds), not a pinned deterministic fixture — a synthetic two-tile map asserting "industrial faction strictly prefers the mountain tile" would be tighter. PRIOR session below.

**Session date:** 2026-06-29 (prev) · **Repo:** aslater100/Centuria · **Tests:** 1140 passing · tsc clean · serialize-determinism green · save-size green · headless 8-seed×181y finite/stable (infl 2.0%, pop 19.0–21.7k, treas 6.7–12.7 mo of GDP) · **this session: PERSONALITY-STEERED RIVAL BUILDING — the AI's spatial play now DIVERGES by who the rival is (intentional balance re-baseline).** User: *"address the weakness of the ai with AI spatial play (a balance re-baseline)."* The flagged weakness (PART 2 NEXT-a below): rivals played spatially since last session but picked buildings by PURE TERRAIN FIT — a Merchant Republic and a Military Junta on the same land built the identical town, so the spatial buildout never diverged by personality. **Fix — new private `factionBuildLean(faction): Record<SectorId,number>`** (region.ts, ~L14385), a per-sector thumb-on-the-scale derived PURELY from EXISTING serialized faction fields: regime bloc (via `RIVAL_REGIMES[...].bloc` — `liberal`→services+0.6·knowledge, `traditional`→agriculture, `autocratic`→industry, `revolutionary`→0.75·industry+0.5·knowledge, scaled by `BUILD_LEAN_BLOC` 0.08), `techFocus` (`mining`→industry, `forestry`→0.6·industry, `farming`→agriculture, ×`BUILD_LEAN_FOCUS` 0.05), and `aggressiveness ≥ BUILD_LEAN_AGGR_THRESHOLD` 60 → +`BUILD_LEAN_AGGR` 0.04 industry (war economy). Folded into `tryBuildRivalBuilding`'s score: `b.bonus + tileYield[sector] + lean[sector]` (`'all'`-sector buildings get 0 lean). Each term ≤ a strong terrain yield, so the rival still builds to its land — the lean only tips close calls + reorders the build sequence. **KEY: NO RNG, NO new serialized field** — the lean is recomputed each call from fields fixed at faction creation → the main RNG stream is untouched and determinism + save-size gates stay green (the proven discipline). **Re-baseline (8-seed×181y):** outcomes 5d/3dys → **4d/4dys** (seed 1049 flipped drowned→dystopia; seeds 1021/1028/1035/1042 shifted treasury/pop/towns — divergent timelines widen as intended), treas/GDP 6.7–12.7 mo, infl pinned 2.0%, pop 19.0–21.7k, towns 6–35 — bounded/finite, no collapse/balloon. Some seeds (1000/1007/1014) barely move — the lean only changes a town when it tips the terrain-optimal pick, and headless markers are player-centric so rival changes propagate only via competition. New tests in `tests/rival-development.test.ts` (14→27, +13): bloc signature per bloc, unknown-regime→traditional fallback, focus + belligerence nudges (isolated via a liberal bloc that touches neither industry nor agri), stacking, purity, lean-aware `tryBuildRivalBuilding` pick match, **non-vacuous divergence across 30 seeds** (agrarian vs industrial pick differs for ≥1 seed). `refPickBuilding` reference made lean-aware (calls the real `factionBuildLean`) so it stays the single source of truth and the existing pick/cell tests keep passing. **GOTCHA for next session — TWO disjoint id-spaces:** `RivalNation` (`this.rivals`, has `archetype`, diplomacy/relations, looms over a compass edge) and `RegionalFaction` (`this.regionalFactions`, the on-map spatial builders with `settlementIds`/`treasury`, what `updateFactionAI` drives) are SEPARATE — a faction has NO `archetype`, only `regime`/`techFocus`/`aggressiveness`. That's why the lean reads those faction fields, not the archetype. Unifying these is part of the global-world arc below. **NEXT (spatial AI, deeper):** (a) let the lean also steer DISTRICT choice + EXPANSION siting (only building choice diverges today; districts follow the clusters the lean creates, but expansion is still personality-blind); (b) PLAYER-faction auto-develop in autoplay behind an explicit flag (markers are player-centric → a passive player under-exercises its OWN spatial path); (c) tune lean magnitudes after a human playtest.

> ⚠️ **WEAK AREAS / VISION for upcoming sessions (user directive, this session): MAKE THE WORLD GLOBAL, NOT REGIONAL.** *"All players and AI should participate in the same global economy; politics should feel worldwide; climate affects regions but also everyone (same as the real world)."* Today: (1) **economy is per-nation** — prices/`localGoodsScarcity`/arbitrage resolve within a nation's own towns; the global goods shipping (PR-3) exists but autoplay towns stay self-sufficient so it's dormant; there is no single world market clearing price across player+rivals. (2) **politics is two disjoint layers** — `RivalNation` diplomacy (treaties/relations/blocs/climate accords) vs `RegionalFaction` territorial AI; they don't share an id-space, so a "world stage" where every actor trades, allies, and sanctions in one ledger doesn't exist yet. (3) **climate is computed globally but felt narrowly** — `worldGreenShare`/`projectedWarming`/the era verdict run world-wide, and the agri-climate drag hits towns, but the warming doesn't yet bite EVERY actor's economy uniformly (rivals' output, world prices, migration). **The arc:** unify the two id-spaces (or bridge faction↔rival), introduce a world market + worldwide political stage, and make climate a shared shock every actor pays. Large, multi-session; sequence it after the current spatial-AI deepening. This is the new headline structural direction — weigh it before adding another regional-only feature.

**Session date:** 2026-06-29 (prev) · **Repo:** aslater100/Centuria · **Tests:** 1127 passing · tsc clean · vite build green · serialize-determinism green · save-size green · headless 8-seed×181y finite/stable (infl 2.0%, pop ~18–21k) · **this session, PART 2: EMERGENT WORLD GREEN TRANSITION — DIFFERENT TIMELINES TO 2100 (climate re-baseline).** User: "drowned is neutral-to-loss, not a win; AI should be more dynamic so there are different timelines to 2100." Probe confirmed the cause: EVERY autoplay seed funnelled to the `drowned` branch because every force that bends the warming curve (green-tech diffusion, carbon laws, climate accords) is PLAYER-driven, and the autoplay player never even becomes a nation (`stateProclaimed=false` always) → the world ran a pure-fossil rail to proj ~4.4–6.0 °C every run (threshold 2.3). **Fix — the rival WORLD now decarbonizes on its own initiative at a rate that VARIES BY SEED with the rival archetype mix:** new `worldGreenShare()` = mean `ARCHETYPE_GREEN_PROPENSITY` (trading 1.0 / crusader 0.9 / opportunist 0.5 / hegemon 0.2 / hermit 0.1) × a year-ramp (`WORLD_GREEN_START_YEAR` 1972, `WORLD_GREEN_RAMP_YEARS` 38) × warming-urgency (`WORLD_GREEN_URGENCY_C` 1.6, `WORLD_GREEN_BASE` 0.55). It cuts `worldEmissions` (×`WORLD_GREEN_MAX_CUT` 0.92), diffuses into a passive player's own `playerEmissions` (`PLAYER_GREEN_DIFFUSION` 0.6, GDD §5.6 proven-tech diffusion), and credits the era verdict (`decideBranch`: `proj -= worldGreenShare × DROWNED_GREEN_RELIEF` 1.5 — a transitioning world's FLAT projection overstates 2100 warming, so a green century escapes the Drowned sky). **Result: the 8-seed sweep now lands 5 drowned / 3 dystopia** (greener archetype draws → the "Neon Century" instead of drowning) — genuinely different timelines, **deterministic** (archetypes fixed at worldgen; NO new RNG, NO new serialized field → determinism + save-size gates green). In autoplay the spread is drowned↔dystopia (the player is never democratic, so solarpunk stays earned by real play — correct). Warming 2100 fell from ~3.8–5.2 to ~2.5–3.4 and now VARIES; economy stays stable (infl 2.0%, pop 18.5–21.6k, treasury bounded). New `tests/climate-dynamism.test.ts` (10). **NEXT (dynamism):** (a) richer archetype behaviour — let archetype also steer rival BUILDING choices (industrialist→industry, ecologist→services/clean) so the spatial buildout itself diverges; (b) a "green bloc" of rivals forming autonomous climate accords (the accord machinery exists but only player-mediated today); (c) make the player able to reach solarpunk in autoplay only via genuine progress (statehood path). **PART 1 (same session): AI SPATIAL PLAY — rivals build & zone (see below).**

**Session date:** 2026-06-29 (latest, part 1) · **Repo:** aslater100/Centuria · **Tests:** 1117 passing · tsc clean · vite build green · serialize-determinism green · save-size green · bench PASS (worst tick fits a frame) · headless 8-seed×181y finite/stable (infl 2.0%, pop ~18–21k, treas 3.7–12.6 mo of GDP) · **this session: AI SPATIAL PLAY — RIVALS NOW BUILD & ZONE (the #1 structural weak area, CLOSED · intentional re-baseline).** The flagged gap: *every* spatial feature (terrain-match, district adjacency, district-zones, Wonders, all previews) was player-only → **dormant in autoplay**. Root cause found by probe: in autoplay NO faction ever raised a regular building or zoned a district — over 181y the ONLY construction was ~5 rival Wonders (0 player buildings, 0 districts), so the headless balance suite tested a near-empty-building economy running on base output + raw terrain yields. **Fix — rivals develop their towns each AI update** (`maybeDevelopRivalTown` in `updateFactionAI`, rival-only so HUMAN PLAY IS 100% UNTOUCHED): it raises the era-ready building that best FITS the town's land (`def.bonus + tileYield[sector]`) on the hex that MAXIMIZES the realized spatial bonus (new `bestPlacementCell` — terrain-match + same-sector clustering + adjacent-district lift, via the existing `placementPreview`), and zones a district once a ≥2 same-sector cluster exists to reward it. **Funded ONLY from the surplus above a famine floor** (`RIVAL_DEV_RESERVE_MONTHS` 0.5 mo of output — measured: emergency grain runs ~pop·0.45/mo ≈ ~0.01 mo of output, so the floor is ~50× the grain draw → CANNOT death-spiral; the famine-buffer lesson behind `rivalStateCost`). Runs BEFORE the state-cost skim so it spends the fresh tax surplus (the skim otherwise holds the hoard at the 1.5-mo reserve, leaving nothing to build with). **aiRng-gated** (`RIVAL_BUILD_CHANCE` 0.5 × techMult) → MAIN RNG STREAM UNTOUCHED. Refactored `wonderEraYear`→ shared `prereqEraYear(prereq?)`. **Re-baseline verified healthy:** rivals now build ~50+ placed buildings + ~13 districts/run (was 5 wonders, 0 districts); player markers shift via competition but stay bounded & finite across 8 seeds (infl pinned 2.0%, pop stable ~20k, treasury 3.7–12.6 mo of GDP, no collapse/balloon). serialize-determinism + save-size green (rivals' new construction/placedBuildings/placedDistricts round-trip; save doesn't balloon). New `tests/rival-development.test.ts` (14: era gate, bestPlacementCell max/tie/full, terrain-fit pick + smart cell + real cost, max gate, **famine-floor never-spend (build+zone)**, district needs cluster + era gate, integration buildout, determinism). **NEXT:** (a) consider PLAYER-faction auto-develop in autoplay too (headless markers are player-centric — rivals re-baseline them only via competition; a passive-player autoplay still under-exercises the player's own spatial path; gate behind an explicit autoplay flag so live human play stays manual); (b) tune `RIVAL_BUILD_CHANCE`/`RIVAL_DEV_RESERVE_MONTHS` after a human playtest; (c) economy track — 44-good catalog (16-good MVP-18 today; re-baseline-risk, own session); still wants a **human downturn playtest** (goods coupling now has a better chance to bite: rival towns specialise spatially → cross-sector strands possible). PRIOR session below.

**Session date:** 2026-06-29 (prev) · **Tests:** 1103 passing · tsc clean · vite build green · serialize-determinism green · headless 3-seed×60y finite/stable (infl 2.0%) · **this session: PLACEMENT PREVIEW NOW COUNTS THE DISTRICT-ZONE LIFT (spatial accuracy).** `placementPreview(townId,cell,defId)` reported a building's terrain pulse + marginal district-ADJACENCY synergy, but OMITTED the marginal district-ZONE lift — siting a same-sector building on a hex adjacent to a zoned district raises that district's `districtZoneBonus` (+`DISTRICT_ZONE_BONUS` 0.05/adjacent building, capped 3), yet the preview never told the player, so a hex next to your farming district read the SAME as one far away. Fixed: added a `zoneBonus` field to `PlacementPreview` + computed it as the marginal `districtZoneBonusFrom` delta (recompute the zone bonus with the candidate building hypothetically added, minus current → captures exactly the lift it gives the district, capped). **Refactored `districtZoneBonus` → a pure `districtZoneBonusFrom(districts,buildings,sector)` core** that both the live path and the preview call (the proven `districtBonusByCells` single-source-of-truth pattern); the empty-`placedDistricts` early-return guard is preserved in `districtZoneBonus` → **live tick byte-identical** (autoplay never zones → same early-return path; determinism harness green, headless finite/stable). **Pure/read-only** preview → byte-identical. UI: the placement overlay's `+N%` label/tint reads `pv.total`, which now includes `zoneBonus` → a hex beside your district correctly shows the higher value (render-only, no new code). `districtPlacementPreview` gets `zoneBonus:0` (a district never triggers another district's zone). New tests in `tests/placement-preview.test.ts` (now 18, +5: marginal==BONUS, ==live `districtZoneBonus` delta, non-adjacent→0, wrong-sector→0, inert-without-district; existing "total" assert updated to +zoneBonus). **⚠️ WEAK AREA flagged for next session (see below).** **NEXT:** (a) wire district zoning into rival/AI build logic IF a re-baseline wanted (player-only now); (b) economy track — 44-good catalog (16-good MVP-18 tier today; re-baseline-risk, own session); still wants a **human downturn playtest** (goods coupling DORMANT in autoplay, scarcity 0). **⚠️ STRUCTURAL WEAK AREA:** *every* spatial feature shipped over the last ~8 sessions (Wonders, districts, district-zones, terrain-match, all previews/breakdowns) is **player-only and therefore byte-identical/dormant in autoplay** — so the headless balance suite (the only automated balance signal) tests a DIFFERENT game than a human plays, and the gap widens each session. Closing it needs EITHER (a) the AI using spatial placement (a re-baseline) OR (b) a human downturn playtest; both have been deferred many sessions. This is the #1 thing the next session should weigh before adding *another* dormant player-only feature. PRIOR session below.

**Session date:** 2026-06-29 (prev) · **Tests:** 1098 passing · tsc clean · vite build green · serialize-determinism green · save-size green · headless 4-seed×181y **BYTE-IDENTICAL to base** · **this session: PER-SECTOR OUTPUT-BONUS BREAKDOWN (spatial legibility).** The spatial economy grew rich (terrain yield + terrain-match + district-clustering + district-zone + Wonders) but was OPAQUE after placement — `placementPreview` shows the bonus *before* you build, nothing showed *why* a town produces what it does *now*. Added `SectorBonusBreakdown` + public read-only `sectorBonusBreakdown(townId, sector)` (`region.ts`) decomposing the sector's output bonus into `{ buildings, terrain, terrainMatch, districtAdjacency, districtZone, wonder, total }`. **Refactored `buildingBonus` → a `sectorBonusParts` single source of truth** (`buildingBonus` now returns `…sectorBonusParts(t,s).total`), with the float summation order preserved EXACTLY → **bit-for-bit identical** (proven by the 4-seed×181y headless diff matching HEAD verbatim AND a test asserting `breakdown.total === buildingBonus(t,s)` via `toBe`). **Pure/read-only** (reads the same idempotent caches the tick does; `serialize()` unchanged) → byte-identical. UI (`regionview.ts`, render-only): each Economy-tab sector row gains a `+N%` spatial-bonus badge (only when >0) with a `title` tooltip naming the contributing sources (`spatialBonusTooltip`). New `tests/sector-breakdown.test.ts` (8). Gotcha: cast to a `Priv` type to white-box the private `buildingBonus`/`sectorBonusParts`/component methods, exactly like `district-zones.test.ts`. **NEXT:** (a) wire district zoning into rival/AI build logic IF a re-baseline wanted (player-only now); (b) economy track — 44-good catalog (16-good MVP-18 tier today; re-baseline-risk, own session); still wants a **human downturn playtest** (goods coupling DORMANT in autoplay, scarcity 0). PRIOR session below.

**Session date:** 2026-06-29 (prev) · **Tests:** 1090 passing · tsc clean · vite build green · serialize-determinism green · save-size green · headless 4-seed×181y **BYTE-IDENTICAL to base** · **this session: DISTRICTS AS A PLACEMENT CATEGORY (the remaining big spatial rock).** Phase D slice 2 gave *emergent* districts (same-sector buildings on adjacent hexes earn an adjacency synergy). This session makes a **DISTRICT its own placed entity** — a themed zone the player sites on a worked-ring hex, distinct from a building. New `DistrictDef` (loaded from a `"districts"` array in `region_buildings.json`: Farming/Industrial/Commercial/Research, one per sector, `max` 1/city, prereq-gated where apt — research_campus needs `computing`, industrial_park needs `steel_industry`). Effect via new private `districtZoneBonus(t,sector)` wired into `buildingBonus` at the same seam as the other Phase C/D bonuses: each hosted district grants its sector a **flat `def.bonus` (+5%) PLUS `DISTRICT_ZONE_BONUS` 0.05 for each same-sector building on an adjacent hex, capped at `DISTRICT_ZONE_CAP` 3** — so the reward is to zone amid your matching cluster. **Player-only & additive → BYTE-IDENTICAL in autoplay** (the AI never zones; `districtZoneBonus` early-returns 0 when `placedDistricts` is empty → zero hot-path cost; headless 4-seed×181y matches HEAD treasury/GDP/pop/infl exactly — the proven Wonders-slice-1 pattern). Districts live in a **separate `Settlement.placedDistricts` array** (so the building-bonus loops never pull one in), occupy their hex (mutual exclusion: `canPlaceBuildingAt` now also excludes district cells — empty-array no-op in headless), pay upkeep (`buildingUpkeep`, player-only), and take effect **on placement** (instant zoning — minimal serialized footprint: one `placedDistricts: []` per settlement, round-trip lossless + serialize fixed-point verified). Public API: `placeDistrict`/`districtBuildCheck`/`districtCost`/`districtPlacementPreview` (pure, mirrors the live zone calc). UI (`regionview.ts`/`style.css`, render-only): a **DISTRICTS** section in the city panel arms a placement mode (`.armed` button state); legal hexes tint by themed sector with a `+N%` preview label; placed zones draw as a translucent themed hex under the building icons. New `tests/district-zones.test.ts` (16: empty→0 all sectors, flat bonus, per-adjacent reward, cap at 3, same-sector-only, treasury debit, occupancy both ways, per-city max, prereq gate, treasury gate, preview==live, serialize round-trip + fixed-point, determinism, integration output-lift). Gates: tsc clean, vite build green, **1090 tests** (1074+16), serialize-determinism green, save-size green, headless 4-seed×181y **byte-identical to HEAD**, ⚠️ bench-region worst-tick UNRELIABLE in this container as always (zone bonus never runs in autoplay `tick()`). **NEXT:** (a) wire district zoning into rival/AI build logic IF a re-baseline is wanted (currently deliberately player-only); (b) economy track — **44-good catalog** (current is the 16-good MVP-18 tier; a re-baseline-risk change, own session); still wants a **human downturn playtest**. ⚠️ goods coupling still DORMANT in autoplay (scarcity 0). PRIOR session below.

**Session date:** 2026-06-29 (prev) · **Tests:** 1074 passing · tsc clean · serialize-determinism green · save-size green · headless 4-seed×181y **BYTE-IDENTICAL to base** · **this session: PLACEMENT-TIME SITE PREVIEW.** Phase D made placed buildings earn terrain-match + district-synergy bonuses, but the player placed them BLIND — the legal-cell highlight was a flat amber and the only way to learn which hex paid was to build there. Added public `placementPreview(townId, cell, defId): PlacementPreview | null` (`region.ts`): for a candidate cell it returns the `terrainBonus` (the `placedBuildingTerrainBonus` pulse — agri on fertile/river, industry on ore/rough, services on river/coastal; an `'all'` building sums every matching rule) plus the **MARGINAL** `districtBonus` (recompute the town's district synergy WITH the candidate hypothetically added, minus current — so it captures the lift the new building gives its same-sector neighbours, not just what it earns itself) and their `total`. **Pure/read-only** — it clones the cells-by-sector map before adding the candidate, touches no `_districtCache`, mutates nothing → the sim stays byte-identical. To share logic with the live path WITHOUT risking it, the inner district sum was extracted into two private helpers (`districtCellsBySector` + `districtBonusByCells`) that `districtAdjacencyBonus` now routes through **verbatim** (same iteration/summation order → bit-identical; proven by the headless diff + the 9 existing `districts.test.ts`). UI: `drawBuildingPlacementOverlay` (`regionview.ts`, render-only) now tints each legal hex green ∝ `total` and labels it `+N%` over the base amber/gold, so the best site reads at a glance. New `tests/placement-preview.test.ts` (13: validity guards, terrain rule per-cell vs map for agri/industry/`all`, marginal pair == 2 pulses, **districtBonus == live districtAdjacencyBonus delta exactly incl. neighbour-lift**, non-adjacent → 0, mixed-sector → 0, total == terrain+district, purity+determinism). Gates: tsc clean, **1074 tests** (1061+13), serialize-determinism green, save-size green (no new serialized field), headless 4-seed×181y **byte-identical to HEAD**, ⚠️ bench-region worst-tick UNRELIABLE in this container as always (mean tick 0.0107ms unchanged; preview never runs in `tick()`, only on UI render). **NEXT:** (a) deepen spatial — **district as its own placement category** (themed multi-building zones) is the remaining big spatial rock; (b) economy track (44-good catalog — current is the 16-good MVP-18 tier); still wants a **human downturn playtest**. ⚠️ goods coupling still DORMANT in autoplay (scarcity 0). PRIOR session below.

**Session date:** 2026-06-29 (prior) · **Tests:** 1061 passing · tsc clean · serialize-determinism green · save-size green · headless 4-seed×181y **BYTE-IDENTICAL to base** · **this session: DEMAND-AWARE GLOBAL GOODS SHIPPING.** The arbitrage matcher (`tickPriceArbitrage`, `systems/arbitrage.ts`) priced each good per town (slice 3) but dispatched by walking each town PAIR in isolation and shipping that pair's single biggest local gap — so a scarce surplus was split by settlement-array order and a lane could carry only one good. Replaced step-2 dispatch with a **global** matcher: gather EVERY profitable (good, cheap source → dear market) opportunity network-wide, sort largest-price-gap-first (deterministic tie-break good id → source id → market id), then dispatch greedily with a running `shipGoodFrom` debit + a per-directed-lane busy set. Result: the most acute shortage **anywhere** pulls from its cheapest reachable supplier first (a limited surplus reaches the NEEDIEST town, not the earliest-indexed one), and a pair can now carry a **different good each direction** (export what you're flush with, import what you lack). **Step 2 consumes no RNG** (only step-1 delivery/strand logs do, unchanged) → the global ordering can't move the RNG stream. **BYTE-IDENTICAL in balanced play** (proven: 4-seed×181y headless is identical to HEAD treasury/GDP/pop/infl) — confirmed empirically that autoplay opens NO price gaps (towns specialise via Phase C/D terrain yields but stay self-sufficient → `localGoodsScarcity`==0, zero trade flows ever dispatch), so this is a zero-re-baseline correctness upgrade exactly like slice 3 shipped; it only changes WHICH lanes ship under a genuine shortage/specialisation. New `tests/arbitrage-global.test.ts` (6: inert-in-self-sufficient, no-route-no-ship, demand-priority-over-array-order, bidirectional-on-one-pair, determinism, one-shipment-per-lane). **NEXT:** options unchanged — (a) deepen spatial (district as its own placement category / placement-time bonus preview); (b) economy track (44-good catalog — current is the 16-good MVP-18 tier); still wants a **human downturn playtest**. NOTE the goods coupling is DORMANT in autoplay (scarcity always 0) — the high-value follow-up that would make all this machinery BITE is a deliberate specialisation/balance change (re-baseline), which the handoff defers until after a human playtest. PRIOR session below.

**Session date:** 2026-06-29 (earlier) · **Tests:** 1055 passing · tsc clean · serialize-determinism green · save-size green · bench-region worst-tick UNRELIABLE in this container (the unmodified baseline also "fails" — early-colony 3-town stage spikes to 16.7ms with mean 0.004ms; pure GC/scheduler jitter, mean tick cost unchanged) · **this session: WAGNER-STYLE RIVAL TREASURY SINK.** Rivals ran the player's real economy (6% tax on town output) but had NO recurring spending sink, so they banked nearly the whole take and ballooned to ~2 months of (enormous) late-game output (max ~1.25–1.8M gold @2100). Added `rivalStateCost(faction, rivalOutput, periodMonths)` (private, in `region.ts`), debited each `updateFactionAI` right after the tax credit. **KEY FINDING — the rival treasury is the FAMINE SHOCK-ABSORBER:** emergency grain is paid from the faction purse and late-game climate warming drives widespread starvation, so my FIRST design (a flat development-ramped share of output, charged unconditionally) drained the purse to 0 during the late-game famine → grain stopped → **population collapsed (seed 1: 171805 → 334 pop @2100, a death spiral).** Replaced with a **reserve-skim**: spend down only the SURPLUS above a prudent reserve that scales with output (`RIVAL_RESERVE_MONTHS` 1.5 × output), at `RIVAL_SURPLUS_SKIM` 0.25/mo, plus flat `RIVAL_ADMIN_PER_TOWN` 5 — and it **STANDS DOWN entirely when treasury < reserve** (a crisis), so the famine buffer is never touched. Result (4-seed×181y probe): NO collapse, rivals stay strong peers (100k–171k pop, 20–24 towns), hoard capped at ~1.0–1.4 months of output (max ~540–615k @2100, roughly halved). **Intentional re-baseline** — the rival trajectory is CHAOTIC w.r.t. treasury (the surplus partly funded expansion, so trimming it leaves rivals modestly leaner in territory too; every tuning gives a *different* but uniformly-healthy landing — "rivals stay strong peers" is the target, not an exact pop number). New `tests/rival-state-cost.test.ts` (9: reference-formula grid, crisis stand-down, surplus skim, output-scaled reserve, never-negative clamp, period linearity, admin scaling, ballooned-hoard decay, going-concern integration). **NEXT:** options unchanged — (a) deepen spatial (district as its own placement category / placement-time bonus preview); (b) economy track (44-good catalog, demand-aware global shipping); still wants a **human downturn playtest**. PRIOR session below.

**Session date:** 2026-06-28 · **Tests:** 1046 passing · bench 13.8ms · **SPATIAL PHASE D slice 2 — DISTRICTS.** Placed buildings of the same sector on adjacent hexes now form a **district** and earn a clustering synergy bonus (the Civ-6 district hook): each building earns `DISTRICT_ADJ_BONUS` 0.04 per same-sector neighbour, capped at `DISTRICT_ADJ_CAP` 2 per building → a tight cluster pays the most while the total stays bounded and legible. Mixed-sector / `'all'` buildings never form a district. `districtAdjacencyBonus(t, sector)` feeds `buildingBonus` at the **same seam as the Phase C terrain yields**, cached per settlement keyed by placement count (placements only ever grow); transient `_districtCache` NOT serialized. Render: a faint sector-coloured connector glow between adjacent same-sector buildings in `drawPlacedBuildings` (render-only). New `tests/districts.test.ts` (9). **Intentional re-baseline** (auto-sited buildings cluster in autoplay → bonus fires; headless still finite/bounded, inflation 2.0%). **NEXT:** Phase D is essentially feature-complete (Wonders + rival build-race + districts); the spatial track's remaining big rock is **per-hex district BUILDINGS as their own placement category** / a **placement-time district-preview in the UI**, OR pivot to the economy track (44-good catalog, demand-aware global shipping) or the **Wagner-style rival treasury sink** (rivals balloon to ~1.4M late-game — benign but a clean sink follow-up). Also still wants a **human downturn playtest** now that towns specialise + rivals are real economic actors.

---

### PRIOR baton (PR #304 MERGED) — RIVALS RUN THE REAL ECONOMY + Wonder build-race Rivals were perma-weak (≈1 town, ≈100 gold) because their treasury ran on a near-zero abstract formula (`pop·0.002·techProgress` ≈ 0 — a vicious cycle) and their towns were denied player resilience. Now: (1) **rival treasury collects tax (`RIVAL_TAX_RATE` 0.06) from the REAL sector output of its own towns** each `updateFactionAI` — the keystone; rivals reach peer wealth (120k–180k vs player 141k @2000), 12 towns, 100k+ pop; (2) **era-based natural growth applies to ALL towns** (was player-only); (3) **emergency grain drawn from the OWNING faction's purse** (player treasury or rival faction); (4) **`techProgress` capped (`RIVAL_TECH_CAP` 30)** — it feeds `militaryStrength` and uncapped ran to ~4500 → 200×+ army. **Wonder race:** rich/era-ready rivals break ground on unclaimed Wonders via the player's construction pipeline; `wonderClaimed` = first-to-break-ground exclusion (rivals + `cityBuildCheck`); era gate via prereq tech's era-year. **Fixed 3 latent save/load determinism bugs** active rivals exposed: route `cargoPriority` round-trip, `contactedFactionIds` serialization (was re-firing FIRST CONTACT on reload), goal `successCondition` re-attach on load (a function JSON drops) + pinning lazily-created `goodStocks`/`lastEmergencyGrainDay` to a fixed key position. **Perf:** corridor A* heap uses temp-var swaps (14.8ms→7.9ms, result-identical) + per-tick `routePath` memo → bench back under one frame. New `tests/wonder-race.test.ts`; phase-8 notable-death test made phase-robust (was a ~36% rng-phase coin-flip). **NEXT:** rivals lack nation-tier spending sinks so late-game treasury balloons (~1.3M @2081) — benign (tech capped → military pop-bounded) but a Wagner-style rival state-cost sink is the clean follow-up; also wants a human downturn playtest now that rivals are real economic actors.

> Full persistent guide is **`HANDOFF.md`** — its top status block + the
> **"Prioritized non-asset backlog"** section are where the next session picks up.
> This file is the short per-session baton. Read both.
>
> **NOTE — two parallel tracks both live on `main`.** (1) the *economy /
> per-settlement-stocks* track (this baton's main body) — storage swap #294 MERGED;
> **PR-3 slice 1** (goods ride the rails), **slice 2** (per-town supply solve), and
> the four C1 extractions all DONE; **slice 3 — per-good local prices + macro coupling**
> DONE; coupling is inert until towns specialise. (2) the **SPATIAL 4X redesign**
> (`docs/design/spatial-4x-redesign.md`) — Phases A+B merged, **Phase C DONE this session**, Phase D next, §0.

---

## 0. PARALLEL TRACK — SPATIAL 4X redesign (Civ/AoW, "keep the 4X clear")

Steering Centuria toward a **spatial city game** (found towns by clicking, place
buildings/districts/wonders on hexes) **while the 4X stays clear and dominant**
(district-scale like Civ-6, NOT SimCity). Plan: `docs/design/spatial-4x-redesign.md`.

- **Phase A — click-to-found · MERGED (#289).** `canFoundAt`/`foundTownAt`; Found-Town
  button arms placement (green legal-hex highlight).
- **Phase B — place buildings on hexes · MERGED (#291).** `Settlement.placedBuildings`
  + `CityConstruction.cell?`; amber placement ring; `drawPlacedBuildings`. Economy
  byte-identical; old saves migrate via `ensurePlacements`. `tests/placement.test.ts`.
- **Phase D — Wonders · slice 1 MERGED (PR #303).** One-per-EMPIRE
  buildings (`RegionalBuildingDef.unique/empireBonus/empireSector/prestige`) that reuse the
  placement system; effect via `wonderBonus(t, sector)` in `buildingBonus` (routed through
  `empireBonus`, local `bonus:0`, so byte-identical until a player builds one). New serialized
  state `wonderOwner` (wonderId→factionId) + `prestige`; empire-uniqueness in `cityBuildCheck`;
  ownership+prestige on completion; Century-Report telemetry; gold placement highlight.
  5-wonder roster in `region_buildings.json`. 14 tests in `tests/wonders.test.ts`.
  **Phase D — slice 1b — rival-AI Wonder build-race · DONE & MERGED (PR #304).** Built
  on the *rivals-run-the-real-economy* change (same PR) — rivals now fund full-price
  Wonders from real tax income. Intentional headless re-baseline (rivals are now active
  economic actors). **NEXT (Phase D):** **slice 2 — districts** (and a downturn playtest +
  a Wagner-style rival treasury sink — see this session's baton header above).
- **Phase C — per-hex terrain yields + building adjacency · MERGED (#301).**
  `tileYieldFor(t)` iterates the ring (d=1..CITY_WORK_RADIUS), computes agri/industry/
  services bonuses from fertility/ore/roughness/forest/river/coastal, caches per
  settlement (transient, not serialized). `placedBuildingTerrainBonus(t, sector)` adds
  +5% when a placed building's sector matches its cell's terrain. Both feed into
  `buildingBonus(t, sector)`, which `updateSectors` already calls. 17 new tests in
  `tests/terrain-yields.test.ts`. **Intentional re-baseline** (markers shift from prior
  base — grassland towns lean agri, mountain towns lean industry). 1020 tests total.
- **NEXT (spatial):** **Phase D** — districts + wonders. Also: now that towns
  SPECIALISE (Phase C), the PR-3 slice-3 macro coupling (`localGoodsScarcity`) BITES:
  agri-specialised towns strand industry inputs → index > 0 → cost-push + output drag.
  **Re-baseline the economy-balance target and want a human downturn playtest.**
- **Lesson:** AI is the wrong tool for crisp foreground sprites/UI — procedural rendering
  + the spatial layer is the win. `gen:local` (A1111/ComfyUI on 127.0.0.1) is the only
  egress-allowed image path; the `asset-generator` agent wraps it.

---

## 1. TARGET BRANCH

This session worked on `claude/continue-0u355c` (Phase C). A new session
will get its own designated branch; branch it off `main`. Set git identity for new
commits: `git config user.email noreply@anthropic.com && git config user.name Claude`.

> **Env note:** in this web container `origin/main` is a stale/disjoint placeholder
> (~#68); the working branch carries the full real history (the #28x/#29x economy +
> spatial work, 1000+ tests, `Settlement.goodStocks`, the supply solver, the slice-3
> prices). Verify the code is current via `grep localGoodsScarcity src/sim/region.ts`,
> not via `git log origin/main`.

## 2. COMPLETED (this session — Spatial Phase C: per-hex terrain yields + building adjacency)

User said "Continue" → the spatial track's headline next step was **Phase C** (terrain
yields feed the economy, the natural activator of PR-3 slice 3's dormant coupling).

- **`tileYieldFor(t: Settlement)`** (private, in `region.ts`). Iterates all ring cells
  (d=1..CITY_WORK_RADIUS, skipping water / center) and computes:
  - `agriculture`: `(meanFertility − 1.0) × 0.20 + (riverFrac) × 0.06`, clamped [-0.15, 0.25]
  - `industry`: `(roughFrac × 0.10) + (oreFrac × 0.15) + (forestFrac × 0.06)`, clamped [-0.05, 0.25]
  - `services`: `(riverFrac × 0.10) + (coastal ? 0.08 : 0)`, clamped [0, 0.20]
  - `information`: 0 (reserved for later eras)
  Result is cached per settlement-id on `_tileYieldCache: Map<number, ...>` — transient
  private field, NOT serialized (cache is rebuilt lazily on first call after load).
- **`placedBuildingTerrainBonus(t, sector)`** (private). For each placed building whose
  `def.sector === sector`, checks cell terrain: agri building on fertile/river cell → +5%;
  industry building on ore/rough cell → +5%; services building on river/coastal → +5%.
- **`buildingBonus(t, sector)`** extended to add both — `yields[sector]` + adjacency bonus.
  `updateSectors` unchanged (already consumes `buildingBonus`).
- **17 new tests** in `tests/terrain-yields.test.ts`: bounds/finiteness (5 seeds × 4 sectors),
  coastal bonus (seed 2 + `pref: 'coastal'`), cache stability (reference equality + determinism),
  adjacency fires/doesn't-fire rules, integration (output > 0 after 1 month, distinct seeds
  differ, fertile > neutral placement), serialize round-trip, serialization discipline
  (`_tileYieldCache` NOT in JSON).
- **Gates:** tsc clean (only pre-existing node-types error); **1020 tests** (1003 + 17);
  determinism harness ✅; bench-region PASS (worst tick 11.9ms < 16.7ms); vite build green;
  headless 8-seed × 181y stable (all values finite, inflation 2.0%).
- **Key gotcha in tests:** `advanceMonth` must be 1440 ticks (30 days × 48 ticks/day),
  NOT 30 — `monthlyUpdate()` fires on day%30===0, not month. Coastal start: seed 2 +
  `{ pref: 'coastal' }` (foundColony option key is `pref`, not `terrain`).

## 3. IN PROGRESS

- **Nothing half-done.** Phase C is complete and verified.

## 4. FAILED PATHS / GOTCHAS (save the next agent time)

- **`advanceMonth` = 1440 ticks, not 30.** `monthlyUpdate()` fires every 30 days; each day
  is 1440÷30 = 48 ticks (REGION_MINUTES_PER_TICK=30, MINUTES_PER_DAY=1440). A 30-tick run
  is less than 1 day — `updateSectors` never fires → sector outputs are 0.
- **`foundColony` option key is `pref`, NOT `terrain`.** The opts signature:
  `{ pref?: 'river-valley' | 'coastal' | 'highlands' | 'surprise' }`.
  Seeds 3..42 don't produce coastal starts without the pref; seed 2 + `pref: 'coastal'` does.
- **`_tileYieldCache` is transient.** Do NOT add it to serialize/deserialize. Cache is rebuilt
  on first `tileYieldFor` call — determinism is preserved because terrain is static (seed-deterministic).
- **Phase C is an intentional re-baseline.** Headless markers WILL differ from the slice-3 base
  (the prior "byte-identical" baseline). Don't "fix" the deviation — it's the terrain shaping
  economies. The headless suite still passes (stable + finite + bounded), just at shifted values.
- **Phase C activates slice 3's dormant coupling.** When towns specialise (agri town produces no
  industry goods and vice-versa), `localGoodsScarcity` > 0, and cost-push + output drag fire.
  After Phase C lands, re-run the economy-balance tests with a playtest (depression ~1929, war).
  The gate-driven (not stock-magnitude) design stays correct — don't switch it.
- **Two price notions, deliberately distinct.** ARBITRAGE price (`localGoodPrice`) = stock/demand
  based; MACRO index (`localGoodsScarcity`) = gate based. They agree under specialisation, differ
  under a raw shock (gate stays 0 for self-sufficient towns, stock would not).
- **C1 extraction recipe (proven 5×) + the systems/region.ts import cycle** are unchanged.
  Keep new `systems/` value-imports call-time-only. `tradeFlows[].cargo` is the physical-units
  field; dispatch debits the SOURCE, arrival credits the destination.
- **`Settlement.goodStocks` is THE store**; go through the accessors. To seed a town directly in a
  test, poke `(s.goodStocks ??= {})['id']`.
- **`auxRng` is a THIRD serialized RNG** (notable health, loan ids). New incidental stochastic
  detail belongs on `auxRng`.
- **The two gates for any `region.ts` economy change:** the determinism harness
  (`tests/serialize-determinism.test.ts`) and the save-size guard (`tests/save-size.test.ts`).
- **`npm install`:** `ELECTRON_SKIP_BINARY_DOWNLOAD=1 PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
  npm install --ignore-scripts --no-audit --no-fund`. **Headless sweep:** `npm run sim -- 181 8`.
  **Bench:** `npx tsx scripts/bench-region.ts`. **tests/** are NOT type-checked by the build.
- **Asset/audio generation** still network-blocked; `gen:local` is the only egress-allowed image path.

## 5. NEXT STEPS

**Phase C (terrain yields) shipped this session and ACTIVATES the PR-3 slice 3 coupling.**
Towns now specialise — a grassland town becomes agri-dominant, a hills town industry-dominant —
which strands cross-sector goods and finally makes `localGoodsScarcity > 0`. The headline next
steps:

- **Re-baseline and playtest.** Run the headless economy-balance suite and re-pin the new markers.
  Then want a **human downturn playtest** (depression ~1929, a war) to verify cost-push + output
  drag + price-driven arbitrage all behave correctly under specialisation.
- **Phase D — districts + wonders.** Now that buildings have terrain adjacency and the economy
  responds to placement, districts (multi-building themed zones with stacking bonuses) and wonders
  (unique one-off buildings) are the natural next spatial feature. See
  `docs/design/spatial-4x-redesign.md`.
- **Economy track:** the full **~44-good catalog** (current is the 16-good MVP-18 tier);
  **demand-aware (global) shipping** — match each deprived town to a surplus town across the
  whole network, not just the per-pair greedy max-gap dispatch slice 3 ships.

**Still UNVERIFIED-BY-HUMAN** (unit + headless only) — want a late-game/downturn playtest:
- **A** agriculture climate drag (#288) — dials `AGRI_CLIMATE_THRESHOLD`/`_SLOPE`/`_MAX_DRAG`.
- the older Phase-2 graded extraction proxy.

**Other safe/medium tracks** (see HANDOFF.md "Prioritized non-asset backlog"): D2-mil
war-support decay / Front stub → spatial Front (large); FX-writer consolidation; UI/UX era skins
+ Century Graph.
