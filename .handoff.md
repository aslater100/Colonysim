# Session Handoff

**Session date:** 2026-06-26 · **Repo:** aslater100/Centuria · **Tests:** 906 passing · tsc clean · vite build green · headless 8-seed × 181y verified

> The full, persistent development guide is **`HANDOFF.md`** (architecture, roadmap,
> every completed phase). This file is the short per-session baton. Read both.

---

## 1. TARGET BRANCH

`claude/continue-u2u3j0` — the designated working branch (per the environment's Git
Development Branch Requirements; **do not** push elsewhere without explicit
permission). Branched clean off `origin/main`, which already has **PR #276 + #277 +
#278 + #279 merged** — the supply cascade, its GDP drag, the MVP-18 DAG, the
oil-shock-through-chain, and the Supply UI tab.

## 2. COMPLETED (this session — one PR, two commits)

**Graded raw availability (`D1-econ`).** Made the binary raw proxy fractional, in
two layers the user explicitly asked for ("both, in that order"):

1. **Graded solver + partial oil shock (commit `8b700e7`).** New
   `resolveSupplyChainGraded` (`supply.ts`): each raw reports a *level* 0..1, a
   good's level is the **min over its inputs** (Liebig), health is the **mean**.
   Strictly generalises the boolean solver (mean == supplied/active when every
   level is 0/1) → byte-identical in all-or-nothing play. `rawEmbargoes` carries a
   `cut` (`{ until, cut }`; old numeric saves migrate to `cut:1`); the 1970s oil
   anchor cuts oil **partially** (`OIL_EMBARGO_CUT = 0.6`). Stock ledger produces
   `baseOutput × level`; secondary effects scale their RNG draw by the shortfall
   (full cut → identical draw, healthy → none).
2. **Graded extraction proxy (commit `f162559`).** Per-sector trailing EWMA output
   norm (`sectorOutputNorm`, serialized). `sectorRawLevel` grades off output/norm:
   ≥ norm → 1.0 (healthy play byte-clean); a contraction below the deadband (0.9)
   grades toward MIN_LEVEL (0.35) at the floor (0.5). Norm chases output → the drag
   bites the *transition* into a downturn, then heals. Damped on three axes (floor
   ≠ 0, 1-month-lagged mult, 15% max drag) → non-divergent.

Tests: `tests/supply.test.ts` (graded solver), `tests/oil-shock-chain.test.ts`
(partial embargo + migration), `tests/supply-shock.test.ts` (graded proxy). **906
total** (890 → 906). tsc clean, full suite green, build green.

## 3. IN PROGRESS

- Branch is complete + green; push + open the draft PR, await CI.

## 4. FAILED PATHS / GOTCHAS (save the next agent time)

- **Byte-identity hinges on the deadband + the EWMA chasing growth.** In steady
  or growing play output ≥ its trailing norm → `sectorRawLevel` clamps to 1.0 →
  no drag → the suite is unperturbed. The whole 906-test suite passes *with* phase
  2 live precisely because no healthy-play test dips output below the 0.9 deadband.
  If you tighten the deadband or steepen the curve, expect to re-baseline.
- **The graded proxy IS an intentional balance change** — but it's invisible until
  a real contraction (recession/disaster/war), confined to the industry drag
  (≤15%), and self-healing (the norm re-baselines). Verify with the headless sim
  (`npx tsx src/sim/headless.ts 181 8`), NOT just unit tests — a doom-loop would
  show as a seed spiralling to ~0 GDP/pop. None did; large economies barely move.
- **The goods are STILL an abstract layer.** Stocks feed only `supplyChainHealth`
  (→ drag) + the two secondary effects (pharma→plague, electronics→research).
  Nothing reads goods/levels into GDP, prices, or trade. The graded plumbing is
  groundwork for that, not the payoff — wiring real consumption/prices is next.
- **`supplyShockSeverity` reads the *cached* `supplyChainHealth`**, not the live
  snapshot health. To assert a drag in a test you must `tickIntermediateGoods()`
  first; to assert a raw *level* without a tick, use `supplyChainSnapshot()` (pure
  read — it evaluates `sectorRawLevel` but does NOT advance the norm).
- **`npm install`:** `ELECTRON_SKIP_BINARY_DOWNLOAD=1 PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
  npm install --ignore-scripts --no-audit --no-fund` (electron's postinstall 403s
  in this web env and rolls back the whole install).
- **Asset/audio generation** still network-blocked (`huggingface.co` 403, no
  sharp/ffmpeg). Pipeline plumbing is done + dry-run-verified; generate locally.

## 5. NEXT STEPS (immediate, 1–3)

1. **Push `claude/continue-u2u3j0` + open the draft PR → `main`.** Branched clean
   off `main`, no conflicts.
2. **Make goods read into the economy** (the remaining depth step). Stocks/levels
   still feed *only* the drag + two secondary effects; nothing reads them into GDP,
   prices, or trade. The graded substrate (per-good levels, embargo cuts, sector
   norms) is now in place to build real consumption/prices/physical-goods-on-routes
   on top of.
3. Alternatively pick up `C1` (region.ts modularization) — `supply.ts` + the
   `supply*` / `rawSupplyLevel` / `sectorRawLevel` / `supplyChainSnapshot` methods
   are the free-function template.

**UI note:** the Supply tab (`regionview.ts::supplyPanelHtml()`) now shows the
embargo's cut depth and renders partially-supplied goods amber (◐ "running at N%")
distinct from a total cut. Eyeball it via Economy (E) → Supply with an active
embargo, or by dropping a sector's output below its `sectorOutputNorm`.
