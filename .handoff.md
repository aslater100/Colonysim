# Session Handoff

**Session date:** 2026-06-27 (late) · **Repo:** aslater100/Centuria · **Tests:** 971 passing · tsc clean · vite build green · headless 8-seed × 181y all finite

> Full persistent guide is **`HANDOFF.md`** — its top status block + the
> **"Prioritized non-asset backlog"** section are where the next session picks up.
> This file is the short per-session baton. Read both.
>
> **NOTE — two parallel tracks both merged into `main` now.** (1) the *economy /
> per-settlement-stocks* track (this baton's main body); (2) the **SPATIAL 4X
> redesign** (`docs/design/spatial-4x-redesign.md`) — Phases A+B now merged, see §0.

---

## 0. PARALLEL TRACK — SPATIAL 4X redesign (Civ/AoW, "keep the 4X clear")

Steering Centuria toward a **spatial city game** (found towns by clicking, place
buildings/districts/wonders on hexes) **while the 4X stays clear and dominant**
(district-scale like Civ-6, NOT SimCity). Plan: `docs/design/spatial-4x-redesign.md`.

- **Phase A — click-to-found · MERGED (#289).** `canFoundAt`/`foundTownAt`; Found-Town
  button arms placement (green legal-hex highlight).
- **Phase B — place buildings on hexes · MERGED (#291).** `Settlement.placedBuildings:
  {id,cell}[]` + `CityConstruction.cell?`; build button arms amber placement in the
  `CITY_WORK_RADIUS=2` ring; `drawPlacedBuildings` renders sector-tinted hex icons.
  **Economy byte-identical** (`buildings[]` stays the `buildingBonus` source); old saves
  migrate via `ensurePlacements` (no RNG). `tests/placement.test.ts`.
- **NEXT (spatial):** **Phase C** — per-hex terrain yields + building adjacency feed the
  economy at the `buildingBonus` seam (*intentional* re-baseline) → **Phase D** districts
  + wonders. Playtest 4X clarity each phase.
- **Lesson:** AI is the wrong tool for crisp foreground sprites/UI — procedural rendering
  + the spatial layer is the win. `gen:local` (A1111/ComfyUI on 127.0.0.1) is the only
  egress-allowed image path; the `asset-generator` agent wraps it.

---

## 1. TARGET BRANCH

This session worked on `claude/gallant-planck-6eze8y` — **merged (#292), branch synced to
`main` at `0800db0`**. A new session will get its own designated branch; branch it off
`main` (which now contains #291 + #292). Set git identity for new commits:
`git config user.email noreply@anthropic.com && git config user.name Claude`.

## 2. COMPLETED (this session — 1 PR merged)

User asked "what's next (per-settlement-stocks foundation)?" → planned the multi-PR
rework of the goods ledger, then shipped the foundation step:

- **#292 — goods-stock ledger seam (the per-settlement-stocks FOUNDATION).** Routed every
  read/write of the nation-wide `intermediateGoodStocks` pool through a small accessor API
  on `RegionSim` so the later storage swap is surgical, not a scatter-edit:
  - **reads:** `goodStock(id)` (0-default), `hasGoodStock(id)` ("tracked?" predicate).
  - **writes:** `produceGood(id,qty)`, `drawGood(id,qty)` (floors at 0; **no-op for an
    untracked raw** — the raw is proxied by its extracting sector, not held in the ledger),
    `seedGoodStock(id)` (seed-to-0; **never overwrites** an existing stock).
  - **save:** `goodStocksSnapshot()` / `restoreGoodStocks(d)` — the serialize/deserialize legs.
  - Touch points routed: the `tickIntermediateGoods` production loop, `rawSupplyLevel`,
    serialize, deserialize. (UI reads the ledger only through the sim — nothing else changed.)
  - **Byte-identical, verified 3 ways:** determinism harness ✅, save-size guard ✅, and a
    direct same-seed `serialize()` snapshot **hash-for-hash identical to base** across 3
    seeds × 5 era checkpoints (1950–2070) with the ledger populated (15–16 goods).
  - +7 unit tests pinning the accessor contract (incl. the two subtle invariants above).

Tests 964 → **971** (incl. #291). Every change byte-identical.

## 3. IN PROGRESS

- **Nothing half-done.** #292 merged; branch synced to `main`.

## 4. FAILED PATHS / GOTCHAS (save the next agent time)

- **`stockOf` was already taken** — it's the per-town food/wood trade-market reader
  (`stockOf(t: Settlement, g: TradeGood)`). The intermediate-goods ledger read is
  **`goodStock(id)`**. Don't re-collide.
- **`intermediateGoodStocks` is STILL ONE nation-wide pool** — the seam (#292) only
  *wrapped* it; the per-settlement storage swap is PR-2 (see §5). Go through the
  `goodStock`/`produceGood`/`drawGood`/… accessors, never the raw record.
- **`drawGood` no-ops on an untracked good on purpose** — that's how a raw input
  (coal/iron/…) stays proxied by `sectorRawLevel` instead of being consumed from stock.
  Preserve this when moving to per-settlement (a raw with no entry must not be created).
- **`auxRng` is a THIRD serialized RNG** (notable health, loan ids) — separate from
  `rng`/`aiRng`. Any new incidental stochastic detail that must not perturb gameplay
  belongs on `auxRng`. (`advisorForecast` keeps `Math.random` on purpose — UI-only.)
- **The determinism harness (`tests/serialize-determinism.test.ts`) compares FULL
  `serialize()`** across two same-seed runs + canonical reload — the gate for every
  "byte-identical" claim. Run it after any region.ts change. The **save-size guard**
  (`tests/save-size.test.ts`, century ceiling 192 KiB, ~82 today) is the other gate —
  it exists precisely for per-settlement bloat (44 goods × N towns), so PR-2 must re-check it.
- **C1 extraction recipe (proven 2×):** move the body verbatim (`this.`→`r.`) into
  `systems/<name>.ts` as `export function tick<Name>(r: RegionSim)`, `import type
  { RegionSim }`, `tick()` calls it. Needs every touched `r.x` PUBLIC. Verify with the harness.
- **`DAYS_PER_MONTH = 5`** (≈60-day year ÷ 12) — not 30. A `tick()` is sub-day; ~1952
  ticks/game-year, so loop `while (r.year < target) r.tick()` rather than counting ticks.
- **`npm install`:** `ELECTRON_SKIP_BINARY_DOWNLOAD=1 PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
  npm install --ignore-scripts --no-audit --no-fund`.
- **Asset/audio generation** still network-blocked (`huggingface.co` 403, no sharp/ffmpeg);
  `gen:local` (A1111/ComfyUI on 127.0.0.1) is the only egress-allowed image path.

## 5. NEXT STEPS

**The per-settlement-stocks rework (the foundation #292 just laid is step 1):**

- **PR-2 — per-settlement storage, aggregate-equivalent (still byte-identical gameplay).**
  - Add `Settlement.goodStocks?: Record<string,number>` (optional → trivial old-save backfill).
  - **Distribute, don't re-solve:** keep the chain solving once on the nation aggregate, but
    split each tick's `baseOutput × level` across towns by producing-sector output (industry
    for manufactured, agriculture for food). Same total → solver reads an identical aggregate
    → health/shock/RNG byte-identical. This is the trick that makes the storage move safe.
  - `rawSupplyLevel` reads the **sum** across towns (a derived nation total).
  - Serialize per-town; backfill a legacy save by depositing the old nation pool into the
    capital. **Re-check the save-size guard** (sparse-serialize non-zero entries; bump the
    ceiling deliberately + note it if genuinely needed). Iterate settlements in stable id order.
  - Reach goods-producing eras in tests via `while (r.year < target)` (see §4 tick note).
- **THEN (intentional divergence, own playtested PRs):** goods consumed where produced →
  surplus shipped on the existing route network (extend the #288 `pendingIncome` delivery to
  move physical units) → local shortages → **per-good local prices** (drop the wage-gap
  proxy) → the full ~44-good catalog.

**Still UNVERIFIED-BY-HUMAN** (unit + headless only) — want a late-game/downturn playtest,
but per-settlement stocks is structural and does NOT build on them, so it isn't blocked:
- **A** agriculture climate drag (#288) — dials `AGRI_CLIMATE_THRESHOLD`/`_SLOPE`/`_MAX_DRAG`.
- the older Phase-2 graded extraction proxy.

**Other safe/medium tracks** (see HANDOFF.md "Prioritized non-asset backlog"): more C1
extraction; D2-mil war-support decay / Front stub → spatial Front (large); FX-writer
consolidation (two writers fight); UI/UX era skins + Century Graph. The spatial-4X track
(§0) is the other live thread — Phase C next.
