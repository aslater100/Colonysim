# Session Handoff

**Session date:** 2026-06-27 · **Repo:** aslater100/Centuria · **Tests:** 964 passing · tsc clean · vite build green

> Full persistent guide is **`HANDOFF.md`** — its top status block + the
> **"Prioritized non-asset backlog"** section are where the next session picks up.
> This file is the short per-session baton. Read both.
>
> **NOTE — two parallel tracks merged here.** The baton below is the
> *non-asset-backlog* track (`claude/sharp-bell-yb4f7f`). A second track
> (`claude/continue-u2u3j0`) ran the **SPATIAL 4X redesign** — see the box just below
> and `docs/design/spatial-4x-redesign.md`.

---

## 0. PARALLEL TRACK — SPATIAL 4X redesign (Civ/AoW, "keep the 4X clear")

User is steering Centuria toward a **spatial city game** (found towns by clicking,
place buildings/districts/wonders on hexes) **while the 4X stays clear and dominant**
("4X clarity is most important"; district-scale like Civ-6, NOT SimCity). Plan:
`docs/design/spatial-4x-redesign.md`.

- **Phase A — click-to-found · MERGED (#289).** `canFoundAt`/`foundTownAt`; Found-Town
  button arms placement (green legal-hex highlight). Territory/borders already existed
  (`computeTerritoryGrid`/`drawTerritories`).
- **Phase B — place buildings on hexes · PR #291 OPEN (`claude/continue-u2u3j0`).**
  `Settlement.placedBuildings:{id,cell}[]` + `CityConstruction.cell?`; build button arms
  amber placement in the `CITY_WORK_RADIUS=2` ring; `drawPlacedBuildings` renders
  sector-tinted icons on hexes (also addresses "town art is weak"). **Economy
  byte-identical** (`buildings[]` stays the `buildingBonus` source); old saves migrate
  via `ensurePlacements` (no RNG). `tests/placement.test.ts`.
- **NEXT (spatial):** merge #291 → **Phase C** (per-hex terrain yields + building
  adjacency feed the economy at the `buildingBonus` seam — *intentional* re-baseline) →
  **Phase D** (districts + wonders). Playtest 4X clarity each phase.
- **Also this track:** `gen:local` (free LOCAL AI image tool — A1111/ComfyUI on
  127.0.0.1, the only egress-allowed path) + the `asset-generator` agent. Lesson: **AI
  is the wrong tool for crisp foreground sprites/UI** — procedural rendering + the
  spatial layer is the win. Visual-polish Phase 1 (sprite armies + shaded towns) = #287.
- **UI hooks:** map-click mode branches (placement → founding → scout → select →
  claim-land); per-frame overlays draw map-space under the camera after the mapCache
  blit; territory is baked into the static mapCache (dynamic overlays must be per-frame).

---

## 1. TARGET BRANCH

`claude/sharp-bell-yb4f7f` — the designated working branch (do not push elsewhere
without explicit permission). Currently clean at the merged `main` (`39390c7`).
**All of this session's work is merged.** Set git identity for new commits:
`git config user.email noreply@anthropic.com && git config user.name Claude`.

## 2. COMPLETED (this session — 8 PRs, all merged)

Brief evolved: "continue the build (not assets)" → "finish the rest of the non-asset
work" → user-picked Tier-2/Tier-3 (A+B). Ran an 8-reader+synthesizer **inventory
workflow** first to ground-truth the roadmap, then shipped down the backlog:

- **#283 — cost-push inflation** (D1-econ prices leg): a supply shock lifts the
  monetary inflation target → the 1973 oil shock is finally stagflation.
- **#284 — non-asset depth pass (4 commits):** trade-leg **export drag** (the 3rd
  goods→economy leg; GDP·prices·trade now all wired) · **serialize-determinism
  harness** + 3 real bug fixes it caught (5 `Math.random` non-determinism → a new
  serialized **`auxRng`**; unserialized one-month-lagged supply caches; lossless save
  round-trip via `cityServiceFields()` + spread-first notable backfill) · first **C1
  extraction** `tickPollution` → `systems/pollution.ts` · **`bench-region`**
  re-baselined to the wall-clock frame model.
- **#285 —** C1 extraction `tickServiceCoverage` → `systems/services.ts` · **situation-
  aware deals** (`rivalSituation`, embattled rivals sign protection/trade more readily).
- **#286 —** difficulty-scaled rival belligerence (`aggroChance` reuses the existing
  `aiAggression` knob; byte-identical on normal) · intel-gated agenda display.
- **#288 — two intentional BALANCE changes (user picked A+B):**
  - **A** agriculture climate drag (`agriClimateMult`: warming >+1.5°C trims the farm
    SECTOR's GDP, ≤30%, distinct from the +0.8°C subsistence-food drag).
  - **B** physical goods on routes, first slice (trade flows now DELIVER —
    `pendingIncome` paid on arrival after transit, severed routes strand cargo,
    inverted buy/sell direction bug fixed).

Tests 920 → **950**. Every change byte-identical unless explicitly a balance change.

## 3. IN PROGRESS

- **Nothing half-done.** All work merged; branch clean at `main`.

## 4. FAILED PATHS / GOTCHAS (save the next agent time)

- **`auxRng` is a THIRD serialized RNG**, separate from `rng`/`aiRng`, so previously-
  `Math.random` draws (notable health, loan ids) became deterministic WITHOUT shifting
  the main/AI streams. Any new incidental stochastic detail that must not perturb
  gameplay belongs on `auxRng`. (`advisorForecast` keeps `Math.random` on purpose —
  UI-only display noise; seeding it from a serialized RNG would let re-renders corrupt
  save determinism.)
- **The determinism harness (`tests/serialize-determinism.test.ts`) compares FULL
  `serialize()`** across two same-seed runs + canonical-form reload — it's the gate for
  every "byte-identical" claim and every C1 extraction. Run it after any region.ts change.
- **C1 extraction recipe (proven 2×):** move the body verbatim (`this.`→`r.`) into
  `systems/<name>.ts` as `export function tick<Name>(r: RegionSim)`, `import type
  { RegionSim }` (no runtime cycle), `tick()` calls it, update tests that cast-and-called
  the private method. Needs every touched `r.x` PUBLIC. Verify with the harness.
- **`DAYS_PER_MONTH = 5`** (≈60-day year ÷ 12) — not 30. Monthly tick deltas use it.
- **`computeCongestionTariff` returns 0.3 for BOTH "no route" and "max-congested
  route"** — don't use the tariff to detect a severed lane; test route *existence*
  (see tickPriceArbitrage's stranding check).
- **`intermediateGoodStocks` is ONE nation-wide pool**, not per-settlement — the blocker
  for real inter-settlement goods movement / local prices (B's big follow-on).
- **`npm install`:** `ELECTRON_SKIP_BINARY_DOWNLOAD=1 PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
  npm install --ignore-scripts --no-audit --no-fund`.
- **Asset/audio generation** still network-blocked (`huggingface.co` 403, no sharp/ffmpeg).

## 5. NEXT STEPS

**Two intentional balance changes are live and UNVERIFIED-BY-HUMAN** (unit + headless
only) — they want a late-game/downturn playtest before more is built on them:
- **A** agriculture climate drag — dials `AGRI_CLIMATE_THRESHOLD`/`_SLOPE`/`_MAX_DRAG`.
- the older Phase-2 graded extraction proxy (from before this session).

**Biggest open step — per-settlement goods stocks** (the big B follow-on, multi-session):
rework `intermediateGoodStocks` from a nation-wide pool to per-settlement, with
production/consumption/serialization per town, so goods truly move between settlements
and form local prices. Then per-good prices (drop the wage-gap proxy) and the full
~44-good catalog.

**Other safe/medium tracks** (see HANDOFF.md "Prioritized non-asset backlog"): more C1
extraction; D2-mil war-support decay / Front stub → spatial Front (large); FX-writer
consolidation (two writers fight); UI/UX era skins + Century Graph. Assets (`B1-art`/
`B2-audio`) need an env with network egress + image/audio tooling (still blocked).
