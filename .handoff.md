# Session Handoff

**Session date:** 2026-06-26 · **Repo:** aslater100/Centuria · **Tests:** 878 passing · tsc clean · bench-region PASS (60fps) · vite build green

> The full, persistent development guide is **`HANDOFF.md`** (architecture, roadmap,
> every completed phase). This file is the short per-session baton. Read both.

---

## 1. TARGET BRANCH

`claude/continue-working-jkyoq8` — the designated working branch (per the
environment's Git Development Branch Requirements; **do not** push elsewhere
without explicit permission). Rebased clean on `origin/main` (which already has
**PR #276 + #277 merged** — the supply cascade and its GDP drag).

## 2. COMPLETED (this session — two PRs)

- **PR #277 (MERGED) — supply-shock output drag (`D1-econ`).** Wired the
  (previously inert) `supplyChainHealth` into industrial output as a small,
  bounded, **era-baselined** drag: `SUPPLY_SHOCK_MAX_DRAG = 0.15`,
  `supplyShockSeverity()` / `supplyShockOutputMult()`, cached in
  `tickIntermediateGoods`, applied to the **industry** sector in `updateSectors`.
  Healthy play byte-identical (severity 0 → mult 1.0); only a genuine raw collapse
  bites. 11 tests in `tests/supply-shock.test.ts`.
- **Goods-graph expansion (`D1-econ`, OPEN PR — this branch).** `INTERMEDIATE_GOODS`
  **5 → 16**: the GDD §5.2 named set as a primary→intermediate→final DAG (added
  lumber, steel, textiles, fuel, electricity, food, clothing, tools, machinery,
  consumer_goods, luxury_goods). The original five keep exact recipes/eras. New
  `AGRICULTURAL_RAWS` (grain/livestock → agriculture) + `EXTRACTIVE_RAWS`
  (wood/coal/iron/copper/oil/stone → industry) proxy raws by extracting sector;
  coal/iron/copper unchanged. **Economy byte-identical** (5-seed golden); only the
  supply-health metric moves at era boundaries. `tests/{phase15,supply}.test.ts`
  updated for the new cascade math (878 total).

## 3. IN PROGRESS

- Goods-expansion branch is complete + green; push + open the draft PR, await CI.

## 4. FAILED PATHS / GOTCHAS (save the next agent time)

- **The goods are an ABSTRACT layer.** `intermediateGoodStocks` feeds *only*
  `supplyChainHealth` (→ the drag) and the two secondary effects (pharma→plague,
  electronics→research). Nothing reads goods/stocks into GDP, prices, or trade.
  So expanding the *count* deepens the cascade graph but doesn't yet make goods
  "do more" economically — that's deliberate groundwork, not the payoff.
- **The drag is binary / catastrophe-only.** A raw flows iff its extracting
  sector's total output > 0, so the cascade + drag only fire on a *total sector
  shutdown* (deep depression / war). In ordinary play the supply chain is
  invisible. Making it bite under *ordinary* pressure (graded availability, or
  routing real shock events through it) is the high-value next step (§5).
- **Verify economy changes with a GOLDEN snapshot, not just the determinism
  test.** `region-longrun`'s determinism test is same-seed self-consistency — it
  won't catch a real behaviour shift. A 5-seed × N-epoch before/after snapshot of
  GDP/treasury/pop/inflation is the tool (it caught a phantom year-boundary shock
  in #277). For the goods expansion, snapshot the **economy only** (omit health/
  stocks — those legitimately change with the richer graph).
- **Adding a good that needs an unproxied raw breaks byte-identity.** Any new
  good's inputs must ground out in raws that `rawAvailable` returns true for in
  healthy play (i.e. in `AGRICULTURAL_RAWS`/`EXTRACTIVE_RAWS`), or it's perpetually
  disrupted → health < baseline → the drag fires in healthy play → economy shifts.
- **Keep the chain switching on at 1920.** `START_YEAR` is 1919; goods unlocking
  at 1919 would be active at founding (where a fresh, un-ticked sim has 0 sector
  output → raws read unavailable → fresh-sim unit tests + founding state shift).
- **`npm install`:** `ELECTRON_SKIP_BINARY_DOWNLOAD=1 PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
  npm install --ignore-scripts --no-audit --no-fund` (electron's postinstall 403s
  in this web env and rolls back the whole install).
- **Asset/audio generation** still network-blocked (`huggingface.co` 403, no
  sharp/ffmpeg). Pipeline plumbing is done + dry-run-verified; generate locally.

## 5. NEXT STEPS (immediate, 1–3)

1. **Push `claude/continue-working-jkyoq8` + open the draft PR → `main`** (goods
   expansion). Clean rebase on `main`, no conflicts.
2. **Make the supply chain matter in ORDINARY play** (the real payoff). Two
   complementary moves: (a) **graded raw availability** — replace the binary
   `sector-output > 0` proxy with a per-raw ratio (extraction capacity / imports
   vs. demand) so ordinary shortages drag output, not just total collapse; and/or
   (b) **route existing shock events through the chain** — the oil-shock anchor
   (§8.1, already implemented as flat effects) should cut `oil` → `fuel` →
   downstream, so the cascade + drag animate it ("the oil shock isn't a popup",
   GDD §5.4). Both are *intentional* balance changes: re-baseline the golden +
   `region-longrun` + `economy-balance` rather than expecting byte-identity.
3. **Surface the supply chain in the UI** (it has *zero* UI today — grep found no
   references). A supply-chain panel / per-good health + the GDD §5.4 "import
   dependency ratio per critical good" would make the whole system legible. Pure
   read, determinism-safe. Alternatively pick up `C1` (region.ts modularization)
   — `supply.ts` / the `supplyShock*` methods are the free-function template.
